# P4 — Final build, comparison gates and handoff

Status: PARTIAL. Measurement implementation and final build verification are complete; the megablast raw-parity gate has two baseline-existing differences. No performance candidate is integrated and no release certification is claimed.

## Final source and artifacts

`final-build/source.json` records158 Rust/source/build/config entries, all equal to `snapshot-v2.json`. The final offline, locked release builds use separate target directories for native command, serial WASI command, threaded WASI command and threaded reactor. `final-build/builds.json` records exact commands, features, statuses and hashes. All four artifacts are byte-identical to the original baseline, independently verified by the auditor. No Rust, host, guard, scheduling, search options or default Node flags changed in the integrated workspace.

## Actual gates

| Gate | Result | Scope |
|---|---|---|
| Python comparison/measurement regressions |36 PASS | `p0-clock-consumer-tests.log`; includes stale/missing/tampered output, paired failures, invalid timeout, early watchdog wake, locale/oracle provenance |
| Real serial-WASI reuse evidence scenarios |4 PASS | `p0-reuse-final-test.log`, archived raw test directory |
| Isolated P3 traceback boundary unit test |1 PASS | `p3-candidate-unit.log`; candidate was subsequently rejected, not integrated |
| Final TBLASTX LC738874/LC738875 evalue10/100/10000 |12 PASS |3 current oracles and native n1/serial n1/threaded n8 for each threshold, exact raw bytes |
| Final real comparison and plot |5 PASS | SicyWSV/CoBV, oracle/native n1+8/serial1/threaded8;5 valid plotted rows, same raw hash |
| Final megablast EDL933/Sakai |11 PASS |1 oracle and10 baseline/final LOSAT configurations |
| Final megablast NZ self and Sakai/MG1655 |2 oracle PASS;20 LOSAT PARITY_FAIL | All baseline/final/target LOSAT bytes agree; current NCBI differs |

The unchanged existing threshold correctness runner fixes its own900 s timeout before execution; no run approached it. Its times are correctness diagnostics, not the300 s adoption benchmark. Realtime steps remain visible in clock diagnostics without replacing CLOCK_MONOTONIC duration. P1's long AP027131/AP027133 gencode4 regression used a current database oracle and3600 s timeout (`p1-long/`); final source/artifact identity preserves that result. No local-subject genetic-code discrepancy is treated as an algorithm failure.

## Unresolved raw differences

NZ megablast has one differing alignment row among454, affecting identity/length/mismatch/gap count with equal reported coordinates, E-value and bit score. Its registered native authority covers task blastn, not megablast. The first causal divergence remains unresolved and no exception is added.

Sakai/MG1655 has the same five-row SOURCE_UNDETERMINED footprint already recorded by the project. Diagnostic comparison with retained PR5 data rows confirms correspondence, but different output formats prevent a raw GateA comparison. Current raw PARITY_FAIL remains intact. `audit-p4.md` records the independently verified NCBI/Rust source owners and limits. Sorting, header removal and numerical normalization are never acceptance transformations.

## Conditional and unrun checks

No production Rust or shared host is adopted. Consequently Rust all-features/clippy/fmt gates, full host lifecycle regressions, and candidate0/7/custom/matrix/retry/many-subject sweeps are not claimed; failed exploration candidates do not proceed to adoption/control expansion. Existing99/145 certification and46 incomplete records remain unchanged. P4 is not an update to the formal GateA/GateB lineage.

## Diff and evidence review

Production: zero integrated source changes, four identical artifacts. Measurement: six existing Python/Shell/JS owners updated. Tests: four regression test files, including two new files. Documentation: tests README, plan progress and staged reports. Generated evidence: manifests, raw outputs/diffs, logs, profiles, isolated source deltas and artifacts under this run directory.

`worktree-preservation.json` verifies frozen files outside the12 task-owned paths and all unrelated pre-existing tracked dirty patches. Untracked files outside the original snapshot were not all byte-hashed; no broader byte-preservation claim is made. The original dirty-patch archive has an exact33-byte trailing nested gzip header, recorded in `worktree-preservation.json`; it remains unmodified, and only that terminal non-patch suffix was excluded when checking unrelated patch preservation. `current-task.patch` is against the initial dirty snapshot, not HEAD. `SHA256SUMS` covers final evidence. Experimental deltas reconstruct against `baseline-source-v2.tar.gz`; raw profiles and transient drivers are archived. The incomplete original snapshot and failed attempts remain immutable.

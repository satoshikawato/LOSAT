# LOSAT v0.1.0 Scope

Last updated: 2026-08-30.

This document is the canonical concise v0.1.0 support table. The three durable
program certification records govern every supported claim. The release itself
remains unpublished until the final release-wide artifact gate is complete.

NCBI BLAST C/C++ source code remains the sole behavior authority. NCBI BLAST+
executables are validation oracles only and must not be used by LOSAT runtime,
build, feature, fallback, or unsupported-feature paths.

## Scope Decisions

| Area | What is supported or evidenced | Governing record | Outside the claim or still pending |
| --- | --- | --- | --- |
| BLASTN `--task blastn` and `--task megablast` | The declared v0.1.0 local query/subject profile: 14 native cases, comprising 13 exact source-defined cases and one Version 1.1 source-underdetermined common-endpoint equal-HSP case. | [`blastn_v0.1.0_certification.md`](release/blastn_v0.1.0_certification.md) and [`PD-BLASTN-HSP-CANONICALIZATION`](product_decisions/PD-BLASTN-HSP-CANONICALIZATION.md) Version 1.1 | `dc-megablast`, database or remote search, options absent from the certified profile, generic BLASTN compatibility, full-manifest serial-Wasm runtime certification, and threaded-Wasm BLASTN. |
| BLASTP / LOSATP | The declared gbdraw P1-P3 local query/subject profiles using standard outfmt 6; nine fresh native cases are `EXACT_TEXT`, repeatable, and cover the certified native thread settings. | [`blastp_v0.1.0_certification.md`](release/blastp_v0.1.0_certification.md) | Database/remote/non-local search, alternate tasks, matrices, gaps, masking or composition modes, ungapped/Smith-Waterman search, non-standard output formats, generic BLASTP compatibility, and threaded Wasm. |
| TBLASTX / TLOSATX | The declared gbdraw P1-P2 local query/subject profiles using standard outfmt 6, per-endpoint genetic codes, and one thread per job; 14 source-defined native cases are `EXACT_TEXT`, and all six declared deviation contracts pass. | [`tblastx_v0.1.0_certification.md`](release/tblastx_v0.1.0_certification.md) | Database/remote search, other output formats, unexercised options, native thread-count equivalence, generic TBLASTX compatibility, and threaded Wasm. |
| Native CLI | Supported candidate for the certified program profiles. | The three program records above establish program behavior only. | Final release commit and tag, artifact provenance and checksums, target matrix, downloaded-artifact smoke tests, and release-wide certification. |
| `wasm32-wasip1` serial command build | Program-specific evidence only: BLASTP P1-P3 raw profiles have certified serial fallback evidence; BLASTN and TBLASTX have the targeted native/serial-Wasm evidence recorded by their certifications. | All three program records above. | General Wasm support, BLASTN full-manifest Wasm runtime certification, untested program cases/options, packaging, browser ABI stability, and threaded execution. |
| `wasm32-wasip1-threads` | Experimental. | No separate merged v0.1.0 certification record. | Supported status and output-order certification. |
| Rust library API | Internal only. | No public API certification record. | A semver-stable public API commitment. |
| Web / embeddable Wasm API | Internal only. | No public ABI certification record. | Stable public ABI, memory-ownership contract, and browser/embeddable packaging support. |

## Approved TBLASTX Deviation

TBLASTX local `-subject` searches intentionally honor `--db-gencode` for subject
translation/search/reporting for every non-default genetic code. This is the
only approved v0.1.0 deviation from NCBI BLAST+ local `-subject` behavior.

This exception is narrow. It does not permit differences in timing, ordering,
scoring, filtering, statistics, pruning, coordinate handling, output fields, or
formatting.

This is the only approved v0.1.0 behavioral deviation.

The BLASTN source-underdetermined equal-HSP classification in
[`PD-BLASTN-HSP-CANONICALIZATION`](product_decisions/PD-BLASTN-HSP-CANONICALIZATION.md)
Version 1.1 is not an additional deviation. It preserves the multiple survivor
representations permitted by NCBI BLAST+ 2.17.0 source while retaining exact
parity for source-defined behavior.

## User-Facing Contract

- LOSAT is a Rust implementation, not a wrapper around NCBI BLAST+.
- NCBI BLAST+ may appear in tests, diagnostics, comparison scripts, and release
  validation artifacts only.
- Unsupported options must fail explicitly or remain outside the documented
  release scope.
- Generated comparison outputs are not release source unless explicitly
  documented as canonical fixtures.

## Remaining Release-Wide Gate

Program certification does not publish v0.1.0. Before a final tag, record in
`docs/release/v0.1.0.md`:

- LOSAT commit.
- NCBI BLAST+ version.
- Rust version and target triple.
- Input FASTA paths.
- LOSAT and NCBI command lines.
- Output paths and checksums.
- Diff summary covering hit count, order, coordinates, raw score, bit score,
  E-value, and selected output format.
- Explicit pass/fail result.

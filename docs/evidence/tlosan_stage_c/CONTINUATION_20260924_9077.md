# TLOSAN Stage C interruption checkpoint after 90778e50

Branch: feature/tlosan-tblastn-v0.2.0. Starting commit for this checkpoint:
90778e505dc09b2b0a7f48d0c419ec65174f2021; original continuation commit:
97ba376499a6c5a28b72c6f45d80a00fb4f002fd.
Pinned NCBI source: 598d8ae6a72b923127ba2fbfaffd48e4c83bfbf4.
Comparison-only tblastn SHA-256:
e3956f1e107a30439d56c8f72fae4267a7d62ebcc16e4f99d4baf2bdf00402e0.

**The user interrupted this session. Stage C remains open; D and E have not
begun. This checkpoint makes no TBLASTN completion, outfmt parity, performance,
release, or certification claim.** The public TBLASTN CLI remains explicitly
unimplemented for Stages C-E. Continue with
[the preceding checkpoint](CONTINUATION_20260924_2E60.md), the
[97BA checkpoint](CONTINUATION_20260924_97BA.md), and the original
[CF29 checkpoint](CONTINUATION_20260924_CF29.md).

## BLOSUM45 / word size 2 profile now compared

The retained [alternate-matrix fixture](alternate_matrix_word2_20260924/)
contains three protein queries, one 362-nt local subject, BLOSUM45, word size
2, threshold 16, window 60, gap 14/2, E-value 10000, SEG off, composition
mode 0, sum statistics off, and genetic code 1. The six NCBI preliminary calls
use ungapped x-drop 21 and gapped x-drop 53; the independent context-cutoff
probe records 0, 0, INT4_MAX, with the third query context invalid.

| NCBI function, input state, and order | Exact saved comparison in Rust |
| --- | --- |
| blast_stat.c:1506-1528,2167-2180,2778-2803,2833-2848: selected matrix bounds and per-context frequency/Karlin setup before lookup | Three context rows match offsets 0/121/192, lengths 120/70/120, and valid flags 1/1/0 |
| blast_aalookup.c:227-245,438-465,562-603; aa_ungapped.c:478-505: matrix, word size, threshold, and ordered lookup candidate pairs | All 558 ordered candidate rows match across six frames |
| aa_ungapped.c:516-614,1089-1155: two-hit extension with word size 2, BLOSUM45, window 60, x-drop 21, cutoff 0 | All 27 WordFinder init HSPs match in score, frame, query/subject seed, starts, length, and order |
| blast_gapalign.c:3924-3927,4057-4091: each context's cutoff, BLOSUM45 score-only gapped alignment in frame/chunk order | All 22 preliminary gapped HSPs match context, score, frame, start/end, gapped start, and order |
| blast_engine.c:539-586,840-850: endpoint purge, frame merge, and append | All six cumulative append snapshots match ordered context, score, frame, query/subject boundaries |

The implementation is still an internal diagnostic path. It adds
matrix/word-length arguments to the shared lookup and TBLASTN seed and
WordFinder functions, and a matrix argument to the TBLASTN gapped diagnostic.
The existing BLOSUM62/word-3 entry points call these with their original
profile. NCBI file/line/snippet comments are directly above the ported Rust
logic. No NCBI executable or library is in LOSAT's runtime or build path.

The comparison test is
algorithm::tblastn::search_gapped::tests::alternate_matrix_word2_gapped_and_append_match_ncbi;
the candidate and WordFinder comparisons have separate focused tests in
search_seed.rs and search_init.rs. The existing traceback replay test for this
fixture still covers positive interval-tree containment after append; it does
not replace a complete-path containment trace.

## Verification at interruption

- Retained fixture outputs.sha256: all 11 entries passed.
- cargo test --lib algorithm::tblastx::lookup::backbone::tests: 11 passed.
- cargo test --lib algorithm::tblastn::search_seed::tests: 12 passed.
- cargo test --lib algorithm::tblastn::search_init::tests: 9 passed.
- cargo test --lib algorithm::tblastn::search_gapped::tests: 19 passed.
- cargo fmt --all -- --check, cargo clippy --lib -- -D warnings, and
  git diff --check: passed.
- A new release build, whole-suite test, long benchmark, and independent audit
  were not run for this interruption checkpoint. The earlier independent
  read-only audit in the preceding checkpoint remains bounded to its tested
  cases. These are not Stage C acceptance gates here.

Reproduce the exact saved comparisons from the crate root:

    cargo test --lib alternate_matrix_word2_candidates_and_contexts_match_ncbi
    cargo test --lib alternate_matrix_word2_wordfinder_hsps_match_ncbi
    cargo test --lib alternate_matrix_word2_gapped_and_append_match_ncbi

The pinned NCBI fixture command and hashes are in the fixture manifest. This
checkpoint did not regenerate its oracle; it verified the retained hashes and
compared the ordered traces. No uncommitted generated long FASTA is needed.

## Stage C work still required

1. Produce complete-path fixtures for start-offset acquisition failure and for
   an HSP deleted by Blast_HSPTest; compare NCBI function inputs, call order,
   and each deletion. The direct API HSPTest boundary oracle alone is
   insufficient.
2. Produce a positive **chunk-local** common-endpoint purge deletion and a
   chunk-path Blast_HSPListAppend cap boundary. The saved cap-3 direct API
   control and the current INT4_MAX append snapshots do not cover those paths.
3. Trace the full ordered candidate stream on a subject exceeding 5,000,000
   translated residues, including masked/separated ranges and chunk boundaries.
   The existing long fixtures compare recorded WordFinder HSPs, range states,
   gapped/merge rows, and append snapshots, not every candidate.
4. Expand multi-query context partition, invalid contexts, ranking, and
   positive interval-tree containment to complete-path NCBI traces. The current
   three-query profiles and traceback replay establish bounded cases.
5. Sweep every Stage C fixture and resolve any first difference against the
   pinned NCBI function, input state, and execution order before declaring C
   complete. Only then build D's function/state/order table for statistics,
   linking, composition adjustment, Kappa redo, ranking, and deletion. Only
   after C and D match, compare E outfmt 0/6/7 local -subject bytes. Code 32
   uses the comparison-only NCBI C++ API; -db statistics and headers are not
   local -subject expected bytes.

Do not expose the public TBLASTN CLI before all three output formats are
complete. Do not claim Stage E, TBLASTN completion, or certification while
C/D/E gates remain open.

# TBLASTN Stage D checkpoint: Kappa mode 2 translated windows

Branch `feature/tlosan-tblastn-v0.2.0`, continuing the partial checkpoint `ca5a9ef0ddb492adf92e8af872758c607e8f8d4f`. Pinned NCBI C/C++ source: `598d8ae6a72b923127ba2fbfaffd48e4c83bfbf4`. This is **partial Stage D evidence**. It does not satisfy D or E acceptance.

The comparison-only [Kappa probe](ncbi_kappa_mode2_trace.c) and [runner](run_ncbi_kappa_mode2_trace.py) record `Blast_RedoOneMatch` inputs/outputs, `Blast_GetPartialTranslation` returns, and `Blast_AdjustScores` mode, pseudocount, rule and ratio. The runner asserts the pinned source and executable hash, reproduces saved local `-subject` stdout, and checks that probe logging leaves ordinary stderr byte-identical. Commands, hashes, and traces are in [kappa_mode2_20260924](kappa_mode2_20260924). The three code-1 default fixtures contain 14 redo calls, 31 partial translations, and 32 score-adjustment calls. These traces are comparison oracles only; NCBI remains absent from LOSAT runtime and build paths.

NCBI `composition_adjustment/redo_alignment.c:80,626-751` creates translated-subject windows with border 200, sorts by location, joins overlapping windows in the same subject frame and query context, sorts alignments inside each window, then sorts windows by subject range. The Rust `windows_from_translated_aligns` port reproduces the ordered partial-translation length/frame calls for the saved hard-SEG and multi-query first redo events. NCBI `blast_kappa.c:1504-1525` and `blast_util.c:428-454,1141-1205` determine nucleotide start, length, frame and translated NCBISTDAA bytes. The Rust `translated_subject_window_sequence` port reproduces the complete translated amino-acid byte strings for every distinct window in those two events, including negative frames. The [focused test](../../../LOSAT/src/core/composition_adjustment/redo_alignment.rs) reads the saved NCBI trace and actual Stage C subject FASTA inputs.

Validation is recorded in [VALIDATION_KAPPA_20260924.txt](VALIDATION_KAPPA_20260924.txt). The translated Kappa callbacks, SEG decision, mode-2 adjusted matrix and alignment redo, identity, final linking/reaping and ordering are still unported or unconnected on the TBLASTN path. The `subject_is_translated` rejection in `blast_redo_one_match_with_workspace` and the public CLI unimplemented error remain in place. Code 32's Stage D behavior still requires a comparison-only NCBI C++ API oracle using `FindGeneticCode(32)`; the pinned CLI is not that oracle. Stage E local `-subject` outfmt 0/6/7 byte comparison remains gated on complete C and D agreement. No D pass, E pass, TBLASTN completion, certification, or performance claim is made.

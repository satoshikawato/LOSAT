# Round 02 — implementation in progress

The user requested further TBLASTX body improvements after the round01 host-only adoption. Wasm/Native1.20 remains aspirational, not an adoption threshold.

All experimental Rust changes remain isolated in `/tmp/losat-tblastx-tier-20260915`; no round02 Rust candidate is adopted yet. Round01 H1 host changes remain in the working tree. The baseline158Rust/config/build inputs were verified unchanged at continuation start.

- C1 (first real group, then parallel tail): cold Mje n8 body35.534→16.955s, but clean compiled-module reuse session1 regressed10.850→12.044s (+11.01%). Rejected; no additional C1 integration.
- C2 (borrowed FnMut real-round boundary): full loop preserved; actual Wasm/V8 hot closure entry verified.352cases/1903rounds match baseline exactly within each target;38existing Native/Wasm evalue-bit transcript differences are preserved. Clean reuse session1 regressed10.330→11.479s (+11.13%). Rejected.
- C2 Native-first and reuse session0 are excluded conservatively: an independent auditor accidentally ran a costly textual comparison during timing; unknown start/exit, confirmed stopped by12:28:24UTC. Session1 started12:28:29UTC and is clean. All original data remains saved; excluded values do not support adoption or rejection.
- C3 (move existing state/copy immutable captures; final counters returned once): rejected; both clean reuse sessions exceed the time guard.352cases, full state/trace-mode comparisons pass per target. Nonzero diagnostic counters at declared subject600001 exactly match baseline on Native/Wasm/trace-on/off.12threaded focused oracle/thread checks pass. Compiled-module reuse data is retained in C3-reuse-first.

All source scripts, build manifests, raw outputs, failure attempts, measurements and generated-code records remain in the scratch directory pending durable bundling. In particular, state harness v2 had an unimplemented external-template substitution; v3 fixed unique link IDs.300000 is a non-default subject-length case, not the >600000 long-counter branch. V4/C3 now explicitly exercises600001 and nonzero counters. No floating-point values were normalized or tolerated.

- C4 (C2 state ownership plus direct mutable-slice arguments): rejected. The original round body/Boolean termination/counters remain unchanged; 352 state cases, Native/Wasm/trace, nonzero >600000 subject counters, and 12 focused threaded raw/thread checks pass. Actual hot function 1367 contains the full body; V8 records Liftoff and TurboFan. Mje compiled-module reuse passed both sessions (+0.19%/+4.54%), but AP self failed both (+8.22%/+5.67%). All memory guards passed. Both input conditions completed their fixed 16 raw-output checks.
- C5 (first-round-only re-entry): rejected. AP self compiled-module reuse passed both sessions (+4.19%/+3.46%). Mje passed session 0 (-14.05%) but failed session 1 (+9.84%). All 32 outputs and memory/thread guards pass. Full 352-case state proof, target-specific floating-point bits, and original termination semantics were independently reviewed. Passing conditions do not override the failed one.
- Follow-up diagnosis found no added bounds check in the dominant C5 helper scan: baseline and C5 both check the current index and use a cached base plus 28-byte stride. Speculative length assertions were not added.
- C6 (C5 plus diagnostic-format outlining): rejected after both Mje reuse sessions failed (+8.34%/+6.18%). Only 29 existing eprintln macro statements move to immediate, monomorphized cold functions; every condition, read expression, branch, counter, and chain collection remains in order. Independent token review confirms the complete algorithm and macro tokens are unchanged after unwrapping these calls. The hot Wasm function shrank from 12248 to 6946 WAT lines (133 to 61 Wasm locals); this is code-generation evidence, not a speed claim. All eight round7 state transcripts (49041 lines each) match baseline within each target. Actual threshold10 target-HSP traces at n1/n8 have identical full 123-line stderr, exercising nine b0=true trace fall-throughs. All 12 focused raw/thread checks pass. No C6 integration measurements follow the failed early gate.

- C7 (baseline plus safe predecessor-slice cursor): current candidate. This is a separate 30-line loop change on the frozen baseline. It uses split_last for the existing decrement and checked prefix truncation for the existing next_larger jump. No allocation, unsafe access, or previous candidate changes. Independent source/emission review confirms exact cursor mapping and actual removal of the sequential helper-index check; the real-jump bound remains. All eight round8 state transcripts, final counter diagnostics, and full target-HSP trace stderr match baseline per target. Twelve focused actual raw/thread checks pass. Both Mje compiled-module reuse sessions pass, with medians improving 9.92% and 0.74%; both AP sessions pass, improving 2.96% and 20.18%. These session-dependent reductions are not a universal improvement estimate. All 32 raw outputs and time/RSS/thread/budget guards passed independent review, completed at 13:57:59 UTC. Six additional Native/serial primary raw checks pass. The fixed cold/body matrix is running exclusively; full adoption remains pending.

- C7 common Native/Wasm implementation subsequently rejected: the fixed cold matrix passes all four threaded conditions (Mje n1 -2.21%, AP n1 -7.18%, both n8 approximately -0.67%), but Mje Native n1 regresses 6.28%, exceeding the 5% guard. Native n8 is +3.54% and passes. The matrix stops before AP Native and both serial conditions. No samples are extended, and no reactor/browser integration is claimed for C7.
- C8 limits the slice cursor to Wasm and restores the original Native index cursor, keeping one shared selection/trace body. First draft Native entire .text and .rodata match baseline exactly, but Wasm lowering changes (threaded code section -38 bytes, serial -46); C7 timing is not carried over to this draft. C8b preserves the C7 while-let syntax and selects only the cursor representation at compile time. Generated-code and state comparisons are in progress; neither draft is adopted.

- C8b rejected as a common Wasm implementation: independent artifact review permits explicitly labeled C7 threaded evidence, and all four fresh Native body controls pass (-0.48%, -1.56%, -1.33%, +3.63%). Plain serial Mje fails (+10.76%); AP serial is not measured. No sample extension.
- C8c limits the prefix cursor to threaded WASI using the existing target cfg plus wasm-threads feature. Its threaded and Native body artifacts exactly match C8b, but the shared while-let driver still changes serial lowering. Fresh serial Mje fails (+13.28%); AP serial is not measured. Full plain-serial state proof and actual primary outputs pass. Rejected.
- During Native emission diagnostics, objcopy --dump-section unexpectedly rewrote four production ELF inputs. All transformed files and original records are preserved. No body-timing ELF or Wasm was affected. Original production bytes were restored by verified copy or identical relink, with hashes matching original build manifests; fresh C8c Native primary checks pass on restored bytes. See native-objcopy-diagnosis.json and native-objcopy-restoration.json. Subsequent ELF inspection uses a read-only Python parser.
- C8d is experimental: one hygienic macro holds the original selection/trace body; two compile-time drivers retain exact original Native/plain-serial while syntax and C7 threaded prefix while-let syntax. Token substitution preserves all 249 non-comment selection tokens. Builds succeed. Macro source locations merge, shifting static addresses, so matching code sizes are not treated as identical artifacts and prior speed results will not be relabeled. Full state, trace, and primary output checks are running before fresh fixed performance guards.

- C8d command gates now pass: 80 fresh cold/body samples cover all 10 primary input/target/thread conditions. Threaded body reductions: Mje n1 3.18%, n8 2.15%; AP self n1 4.31%, n8 1.15%. Both Mje reuse sessions pass (0.36%/9.66% reduction); both AP reuse sessions pass (17.82%/17.05%). All 32 reuse outputs/thread/budget checks pass. No C7/C8b/C8c timing was carried across relocated layouts. Independent command audit verifies actual artifacts, raw bytes and calculations; monotonic/boottime clocks agree, while adjustable realtime disagreement flags remain explicitly recorded.
- Integration: all 75 command parity conditions pass; all 36 stage files at n1/n2/n4/n8 match baseline. Long-code4 Native, serial, threaded n1/n8 all match the frozen DB oracle. Actual 4-kind ABI, API limits/recovery, 144 threaded reactor runs per version, and 19 host tests pass. Rust checks: 625 passed, 0 failed, 3 ignored; release clippy and format checks pass.
- Raw C8d browser lifecycle initially failed on isolated n8 BLASTP with memory access out of bounds; identical baseline lifecycle passed. All failure data is retained. A later bounded diagnostic (40 raw/guarded fresh/history checks) passed, so the exact original BLASTP trap instruction remains unidentified.
- A separate minimal Chrome149.0.7827.55 fixture definitively reproduces memory.copy traps on valid new-page ranges after another worker grows shared memory while the consumer remains in Wasm. Both copy directions fail raw and pass with the existing Node WASI bulk-memory guard; fill passes both. The consumer prepares child instances before root memory growth and compiles raw modules without this guard. This proves a compatibility defect; it is not claimed as proof of the original BLASTP trap location.
- Added a staged browser-artifact preparation wrapper that reuses the unchanged guard and ABI validator, preserves the raw input, refuses existing output/manifest paths, and records input/output/helper hashes. No consumer JS or Rust behavior changed for this preparation. Full prepared C8d actual-consumer lifecycle now passes all four programs/repetition, n8, cancellation/error recovery, total budget8 and pagehide cleanup with zero external requests. Symmetric prepared-baseline QA and separate browser performance remain pending. No round02 Rust file has been applied to the real working tree yet.

- Symmetric prepared-baseline browser QA passed. Fixed prepared C8d browser n8 timing failed on both primary inputs: Mje20.196095→22.476515s (+11.291%); AP36.494350→38.513415s (+5.533%). All16 raw outputs match. No samples were extended. Thus C8d is not adopted as a common Node/browser artifact despite its passing Node command gates.
- C9 adds an explicit `wasm-browser` build marker. The same hygienic macro owns predecessor selection; Node threaded WASI keeps the C8d prefix driver, while browser/native/plain-WASI retain the original indexed driver. The marker does not enable or cap threads. Browser packaging selects the marker, applies the existing shared-memory guard once, records raw/final identities, and omits Node runtime hosts. Cargo web aliases select the browser driver but still produce raw bytes; preparation is a separate packaging step.
- C9 browser state:352cases/1903rounds, trace off/on, all49041 transcript lines match the original indexed baseline including every floating-point bit. Actual C9 prepared browser lifecycle passes all four programs, repeats, n8/total-budget8, cancellation/error recovery and pagehide cleanup with zero external requests.21 Node tests pass, including the new packaging boundary tests. Actual C9 four-kind ABI and small-fixture reactor/limits/recovery checks pass.
- C9 default production Node/serial command/reactor code sections and all non-data sections match C8d exactly.52 data bytes differ per artifact; independent review is checking their Rust source-location ownership. Native `.text` and `.rodata` also match C8d. No whole-artifact equality or fresh C9 speed result is claimed from these comparisons.
- C9 full `cargo test --release` failed during integration-test compilation with conflicting panic strategies/dependency types. The prior625-test pass used the normal test profile. The normal full C9 suite is running, and the release-profile boundary is being checked independently against baseline. Neither failure nor validation scope is hidden.

# Parity and performance evidence

Fill every applicable field. Use `not applicable` with a reason instead of deleting a gate.

```text
Program and task:
Fixture:
Options:
LOSAT commit and target:
NCBI BLAST+ version:
NCBI source file/function/lines:
LOSAT owner:
First observed divergence:
Accepted exception, if any:
Focused comparison command:
Focused byte-diff result:
Broader comparison command:
Broader result:
Native/Wasm comparison:
Baseline benchmark method and samples:
Candidate benchmark method and samples:
Output checksums:
Remaining risk or unsupported scope:
```

Attach or retain the exact diff, commands, and benchmark samples when the work is release-facing. Do not summarize a non-zero diff as parity.

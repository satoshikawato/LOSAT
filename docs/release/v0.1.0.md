# LOSAT v0.1.0 Release Notes

Status: draft, not yet released.

## Release Metadata

| Field | Value |
| --- | --- |
| Release date | TBD |
| Release commit | TBD |
| NCBI BLAST+ oracle version | TBD |
| Rust version | TBD |
| Native target triples | TBD |
| Wasm target triples | TBD |

Historical pre-certification local gate metadata from 2026-06-22 follows for
traceability only. It is not current release evidence and must be refreshed on
the final clean release branch:

| Field | Value |
| --- | --- |
| Local commit under test | `b8771300f8612e050776ad8adc988767e7e58499` |
| Rust compiler | `rustc 1.92.0 (ded5c06cf 2025-12-08)` |
| Cargo | `cargo 1.92.0 (344c4567c 2025-10-21)` |
| Host triple | `x86_64-unknown-linux-gnu` |
| NCBI BLAST+ oracle | `tblastx`/`blastn`/`blastp` 2.17.0+, package blast 2.17.0, build 2025-08-11 |

BLASTN certification metadata from the clean isolated branch on 2026-08-29 is
recorded in
[`blastn_v0.1.0_certification.md`](blastn_v0.1.0_certification.md). The BLASTN
production commit under test was
`458b87dc0bf6e05f8390775f819590de097dce5e`; the fixed official oracle was
`blastn: 2.17.0+` with SHA-256
`33b64bc67d3149cee2459b2f7766b363323df632cf12c099546de00aea9698b5`.

## Scope

| Area | Status |
| --- | --- |
| BLASTN `--task blastn` and `--task megablast` | Supported for the certified v0.1.0 local query/subject profile |
| BLASTP / LOSATP | Supported for the certified gbdraw P1-P3 local query/subject profiles |
| TBLASTX / TLOSATX | Supported for the certified gbdraw P1-P2 local query/subject profiles, including the one approved local-subject `--db-gencode` deviation |
| Native CLI | Supported candidate; program profiles are certified, but final release artifacts and provenance remain pending |
| `wasm32-wasip1` serial | Program-specific evidence only: certified BLASTP raw-profile fallback and targeted BLASTN/TBLASTX native/Wasm equality; not general Wasm support |
| `wasm32-wasip1-threads` | Experimental; not certified for v0.1.0 support |
| Rust library API | Internal only |
| Web / embeddable Wasm API | Internal only |

## Current Program Certification

- **BLASTN:** the declared local `blastn`/`megablast` profile is governed by
  [`blastn_v0.1.0_certification.md`](blastn_v0.1.0_certification.md): 13 exact
  source-defined cases plus one narrowly source-underdetermined common-endpoint
  equal-HSP case in the 14-case contract. The latter is source compatibility
  under Product Decision Version 1.1, not a LOSAT algorithm deviation.
- **BLASTP / LOSATP:** the declared gbdraw P1-P3 local standard-outfmt-6
  profiles are `CERTIFIABLE_AS_IS` under
  [`blastp_v0.1.0_certification.md`](blastp_v0.1.0_certification.md). All nine
  fresh native cases are `EXACT_TEXT` and repeatable; the record also certifies
  serial `wasm32-wasip1` fallback evidence for the raw P1 and P2/P3 profiles.
- **TBLASTX / TLOSATX:** the declared gbdraw P1-P2 local standard-outfmt-6,
  one-thread-per-job profiles are `CERTIFIABLE_AS_IS` under
  [`tblastx_v0.1.0_certification.md`](tblastx_v0.1.0_certification.md). Fourteen
  source-defined cases are `EXACT_TEXT`; all six declared non-default
  local-subject `db-gencode` deviation contracts pass, and the record includes
  native repeatability and targeted serial-Wasm/native equality.

These records do not certify generic BLAST compatibility, database or remote
search, uncertified options, threaded Wasm, public Rust/Web/Wasm API stability,
or final release artifact/tag status.

## Approved Deviation

TBLASTX local `-subject` searches intentionally honor `--db-gencode` for subject
translation/search/reporting for every non-default genetic code. This local
subject genetic-code behavior is approved for v0.1.0 and must not be counted as
a LOSAT parity defect.

No other deviation from NCBI timing, ordering, scoring, filtering, statistics,
pruning, coordinates, or output formatting is approved.

The BLASTN source-underdetermined equal-HSP policy is not an additional
behavioral deviation. NCBI BLAST+ 2.17.0 source permits more than one
edit-script survivor for that comparator-equal tie class. Product Decision
[`PD-BLASTN-HSP-CANONICALIZATION`](../product_decisions/PD-BLASTN-HSP-CANONICALIZATION.md)
Version 1.1 records the narrow source-compatibility contract.

## Verification Commands

Run from the repository root unless noted otherwise.

```bash
cd LOSAT
cargo fmt --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test --all-features
cargo build --release
cargo package --list
cargo publish --dry-run --locked \
  --config 'build.target-dir="/tmp/losat-cargo-publish-target"'
```

Current program-profile certification entry points:

```bash
cd LOSAT
cargo build --release --locked
python3 tests/compare_blastn_parity.py \
  --manifest tests/blastn_parity_manifest.tsv \
  --fresh-paired \
  --paired-output-dir /tmp/losat-blastn-v010-certification/paired-base \
  --losat-bin target/release/LOSAT \
  --ncbi-bin /home/kawato/tools/ncbi-blast-oracle/ncbi-blast-2.17.0+/bin/blastn
python3 tests/certify_blastn_v010.py \
  --manifest tests/blastn_parity_manifest.tsv \
  --paired-output-dir /tmp/losat-blastn-v010-certification/paired-base \
  --exceptions tests/blastn_v010_source_exceptions.tsv

cd ..
python LOSAT/tests/audit_blastp_v010.py \
  --output-dir /tmp/losat-blastp-v010-audit/final-native
python LOSAT/tests/audit_tblastx_v010.py \
  --output-dir /tmp/losat-tblastx-v010-certification/final-native
```

The three durable certification records are the support authorities. Older
broad comparison scripts remain diagnostics and are not substitutes for these
committed gates.

Release-artifact Wasm diagnostics, if artifacts are shipped (these do not
replace the program certification records):

```bash
cd LOSAT
cargo build --release --target wasm32-wasip1 --no-default-features
cargo build --release --target wasm32-wasip1-threads --features wasm-threads

cd tests
bash run_wasm_comparison.sh
cd ../..
bash tests/compare_tblastx_wasm_parity.sh
```

## Parity Evidence

Current support evidence is the merged durable certification record for each
program. The records freeze the oracle identity, candidate identity, profile,
result classifications, repeatability, Wasm evidence, and exclusions.

| Program | Native certification | Serial Wasm evidence | Authority |
| --- | --- | --- | --- |
| BLASTN | 14 cases: 13 `EXACT_TEXT`; one Version 1.1 source-underdetermined equal-HSP contract with identical HSP membership, coordinates, E-values, and bit scores. | One exact case is NCBI/native/Wasm byte-identical; the source-underdetermined case is native/Wasm byte-identical and retains its certified oracle footprint. Full-manifest Wasm runtime certification is pending. | [`blastn_v0.1.0_certification.md`](blastn_v0.1.0_certification.md) |
| BLASTP / LOSATP | Nine gbdraw P1-P3 cases are `EXACT_TEXT` and three-run repeatable. | The declared P1 and P2/P3 raw profiles are byte-identical across NCBI, native LOSATP, command Wasm, and the exercised gbdraw direct path. | [`blastp_v0.1.0_certification.md`](blastp_v0.1.0_certification.md) |
| TBLASTX / TLOSATX | Fourteen source-defined cases are `EXACT_TEXT`; all six and only the declared non-default local-subject `db-gencode` cases satisfy the approved-deviation contract. All 15 required native cases are three-run repeatable. | Three required probes are native/Wasm byte-identical, including the subject-only approved-deviation probe. | [`tblastx_v0.1.0_certification.md`](tblastx_v0.1.0_certification.md) |

The older June 2026 comparison tables were pre-certification diagnostics and
have been superseded as current release evidence. Dedicated certification gates
must govern any final release-readiness refresh.

## Artifacts

Historical local checksums are retained for traceability only. They are not
final artifact metadata; replace them with downloaded workflow artifact
checksums before tagging.

| Artifact | Target | Features | SHA-256 | Smoke test |
| --- | --- | --- | --- | --- |
| `target/release/LOSAT` | Linux native CLI, local build | default | `d8df7652cd44a9e09ba88b656876eb4c48a3abb5841d7f091ffbd372d62f315a` | `target/release/LOSAT --version` returned `losat 0.1.0` |
| `target/wasm32-wasip1/release/LOSAT.wasm` | `wasm32-wasip1` command Wasm, local build | `--no-default-features` | `67163f7a49504866945161014bf51432be5bc57f944804fb04d35d4ffd821350` | Targeted TBLASTX Wasm parity passed |
| `target/wasm32-wasip1-threads/release/LOSAT.wasm` | `wasm32-wasip1-threads` command Wasm, local build | `wasm-threads` | `ae966097330394f00c16ab72e15446a6a8be1c9c6514737571a39a88dfdab486` | Build passed; full threaded Wasm comparison pending |
| `LOSAT-0.1.0.crate` | crates.io source package, local package verify | default | `7e946b2128a56309b338c6ace09fea74759237ffc69cce60191e3dd9e25aefa9` | `cargo package --locked --offline` and `cargo publish --dry-run --locked --allow-dirty` passed |

## Packaging Evidence

Historical pre-release crates.io packaging gate from 2026-06-22:

| Gate | Status | Evidence |
| --- | --- | --- |
| Cargo metadata | Passed | `cargo metadata --format-version 1 --no-deps` confirmed package version `0.1.0` and release metadata fields: MIT license, repository, README, keywords, and category. |
| Package list | Passed | `cargo package --list` produced 302 files and excluded generated comparison output, BLAST database index files, plots, Python cache files, `target/`, and scratch directories. |
| Package assembly | Passed with local target-dir override | `cargo package --locked --offline --config 'build.target-dir="/tmp/losat-v010-package-target-current-cleancheck"'` packaged 302 files, 26.8 MiB unpacked, 7.9 MiB compressed, and verified successfully. |
| Publish dry-run | Passed with local target-dir override | `cargo publish --dry-run --locked --allow-dirty --config 'build.target-dir="/tmp/losat-v010-publish-target-current"'` completed verification and aborted upload only because of dry-run mode. Network access was used only for crates.io registry/index validation. |
| Package checksum | Passed locally, pending final branch refresh | `LOSAT-0.1.0.crate` SHA-256: `7e946b2128a56309b338c6ace09fea74759237ffc69cce60191e3dd9e25aefa9`. Re-record after clean release-branch packaging. |

On this WSL `/mnt/c` worktree, running `cargo package` without a target-dir
override produced `could not learn metadata for ... LOSAT-0.1.0.crate` even
though Cargo created the tarball. The release workflow uses a `/tmp` target-dir
override for `cargo publish --dry-run` to keep the package verification path
independent of the mounted working tree.

## Release Automation

`.github/workflows/ci.yml` runs the normal PR quality gate: format, clippy,
tests, release build, and serial `wasm32-wasip1` build.

`.github/workflows/release-readiness.yml` is a manual gate for release
candidates. Its default `quality` job records the LOSAT commit, Rust/Cargo
versions, package list, native checksum, serial Wasm checksum, release candidate
artifacts, and release-artifact checksums. Optional manual inputs run native
NCBI oracle diagnostics and Wasm parity diagnostics. Those jobs install NCBI
BLAST+ only inside the validation workflow; LOSAT runtime/build/product code
does not depend on NCBI executables or libraries.

## Contributor And Security Policy

`CONTRIBUTING.md` documents the release-candidate contribution rules: NCBI BLAST
C/C++ source is the behavioral authority, behavior changes need NCBI source
references and parity evidence, unsupported behavior must fail explicitly, and
generated comparison output must not be committed as release source.

`SECURITY.md` documents the pre-v0.1.0 support window, vulnerability reporting
route, and the requirement that security fixes must not add NCBI BLAST+
executables, libraries, bindings, or subprocesses as runtime/build/fallback
dependencies.

`RELEASE.md` is the root-level release entry point and points maintainers to the
v0.1.0 readiness plan, release note, final local gate, required NCBI parity gate,
artifact gate, and publish commands.

## Repository Hygiene Audit

Historical pre-certification release-tree audit from 2026-06-22:

| Path | Tracked files | Size | Release policy |
| --- | ---: | ---: | --- |
| `.codex_tmp/` | 443 | 8.0M | Scratch; remove from release source or move evidence into documented artifacts. |
| `LOSAT/tests/losat_out/` | 623 | 701M | Generated LOSAT comparison output; do not ship as source. |
| `LOSAT/tests/blast_out/` | 185 | 155M | Generated NCBI/BLAST comparison output; do not ship as source. |
| `LOSAT/tests/ncbi_out/` | 1 | 5.0M | Generated comparison output; keep only if documented as canonical fixture evidence. |
| `LOSAT/losat_out/` | 12 | 26M | Generated comparison output; do not ship as source. |
| `LOSAT/.tmp/` | 39 | 45M | Scratch traces; remove from release source. |
| `LOSAT/tests/__pycache__/` | 3 | negligible | Python cache; remove from release source. |
| BLAST database indexes under `LOSAT/tests/blast_db/` and `LOSAT/tests/fasta/` | 224 | 2.9M | Generated by `makeblastdb`; removed from the Git index and ignored. |

These paths are ignored for new files. The 1306 tracked scratch/generated files
listed by this audit have been removed from the Git index with `git rm --cached`
and remain only as local working-tree outputs.

The additional BLAST database index cleanup removed tracked
`*.{ndb,nhd,nhi,nhr,nin,njs,nog,nos,not,nsq,ntf,nto}` files from the Git index
while leaving the local generated files on disk. Verification:
`git ls-files` for those pathspecs returns `0`; `git ls-files --others` with
the standard excludes also returns no unignored generated index files.

## Runtime Hardening Evidence

Historical pre-certification native CLI smoke from 2026-06-22:

| Gate | Status | Evidence |
| --- | --- | --- |
| Help/version without inputs | Passed | `cargo test --manifest-path LOSAT/Cargo.toml --test cli_smoke` covers `losat --help`, subcommand help, and `--version`. |
| Unsupported BLASTP option fail-fast | Passed | `--word-size 7` fails with an explicit pure-Rust BLASTP unsupported error before query file I/O. |
| Missing input file error | Passed | BLASTN missing query input reports `failed to open query FASTA` with the path. |
| Malformed FASTA error | Passed | BLASTN malformed query input reports `failed to read query FASTA` and preserves the parser reason. |
| Normal stderr cleanliness | Passed | A small TBLASTX local run writes no stderr when no `LOSAT_*` debug/timing environment variables are set. |

## Historical Full-Test Snapshot

The 2026-06-22 local `cargo test --all-features` snapshot completed
successfully:

- library tests: 390 passed, 0 failed, 1 ignored
- CLI smoke integration tests: 5 passed
- compressed lookup integration tests: 4 passed
- unit test harness: 177 passed, 0 failed, 2 ignored

This is not a substitute for re-running the same gate on a clean release branch.

## Skipped Or Failed Gates

The program-profile certifications are complete. The remaining entries are
release-wide gates and must be refreshed before publishing a release candidate.

| Gate | Status | Reason | Follow-up |
| --- | --- | --- | --- |
| BLASTN v0.1.0 profile certification | Passed | The durable 14-case record has 13 exact source-defined cases and one narrowly source-compatible Version 1.1 case. | Preserve the certified profile and exception boundary. |
| BLASTP v0.1.0 profile certification | Passed | The durable nine-case gbdraw P1-P3 record is `CERTIFIABLE_AS_IS`. | Preserve its local standard-outfmt-6 profile and exclusions. |
| TBLASTX v0.1.0 profile certification | Passed | The durable 20-case gbdraw P1-P2 record is `CERTIFIABLE_AS_IS`; all six approved-deviation contracts pass. | Preserve the sole `db-gencode` deviation and all exclusions. |
| Final clean release-wide quality gate | Pending | Program certification does not replace the final release-commit gate. | Run the required quality, packaging, and artifact workflow on the final release commit. |
| Final native artifact certification | Pending | No final release commit, target matrix, downloaded-artifact checksum, or extracted-artifact smoke test is recorded. | Certify artifact provenance and checksums before tagging. |
| Serial `wasm32-wasip1` release artifact | Pending as a release artifact | Program-specific certification evidence exists, but final package provenance and smoke tests do not. | Keep claims bounded to the three program records and certify the shipped artifact. |
| Threaded Wasm | Not certified | No separate merged certification promotes `wasm32-wasip1-threads`. | Keep experimental or omit it from supported release artifacts. |
| Release tag and publication | Not executed | The release note is still a draft and final metadata is intentionally `TBD`. | Complete final v0.1.0 release-readiness and artifact certification before tagging. |

# TBLASTN Stage D Kappa result update checkpoint — 2026-09-25

Branch `feature/tlosan-tblastn-v0.2.0`; resumed from `490ea5b8f117f952dee164551da9582e0c9e7f5d`. Pinned NCBI source: `598d8ae6a72b923127ba2fbfaffd48e4c83bfbf4`. The [Stage C fixed-input gate](../tlosan_stage_c/STAGE_C_GATE_20260924.md) and [natural 13-subject Kappa continuation](CHECKPOINT_MULTI_SUBJECT_STAGE_D_20260925.md) define the upstream inputs. **Stage D remains partial and has not passed. Stage E has not started. The public TBLASTN CLI remains unimplemented.**

## Function, input and order

Pinned `blast_kappa.c:2494-2515` pops each composition heap into `Blast_HitListUpdate`, then calls `Blast_HSPResultsReverseOrder`. Pinned `blast_hits.c:3243-3297` first appends while the hitlist is below its limit; when full, it sorts each HSP list by E-value, creates the worst-first heap, compares the incoming list, and replaces or discards it. `blast_hits.c:3420-3437` reverses the resulting list array. The new internal Rust `stage_d_results.rs` port follows these functions and retains the HSP lists for downstream work.

The comparison-only [probe](ncbi_kappa_traceback_trace.c) now records every final `Blast_HitListUpdate` input and output, including count, limit, worst E-value, low score, heapified flag, ordered OIDs and every retained HSP's raw score, E-value, coordinates, frame and within-list position, plus each query's result array before and after reversal. The [replay](run_ncbi_kappa_traceback_trace.py) verified unchanged NCBI stdout and stderr for the local `-subject` cases and retained probe SHA-256, binary SHA-256, commands and trace checksums in [the result trace bundle](kappa_result_order_20260925/manifest.txt). The existing `max_target_seqs=2` [fixture replay](kappa_heap_rejection_20260925/run_ncbi_heap_rejection_trace.py) produced a [separate result trace](kappa_heap_rejection_20260925/result_order_20260925/manifest.txt) with ten real hitlist updates and repeated deletion of older lists. NCBI code 32 CLI output was not used as an oracle.

A focused Rust test checks all input and output hitlist states against those traces for the default 13-subject case, hard-SEG case, and the full `max_target_seqs=2` hitlist. It includes the two-HSP subject in the latter and checks both HSPs' retained order and values after each update. The natural 13-subject Rust Stage C→preliminary link/reap→Kappa redo→postredo link/reap→heap test now passes its own Rust-generated HSP lists through the new hitlist port, matching all 11 NCBI updates and the final reversed OID order `12,11,6,5,4,3,2,1,0,7,8`. A separate focused Rust test checks the NCBI three-query result call order: query 0 and 1 each have one result list; query 2 is empty. A natural Rust multi-query final result path remains untested.

This checkpoint covers the measured hitlist boundaries only. The public runtime orchestration, all remaining local fixture/option combinations and all 27 genetic codes, general result post-pipes and edit-script-bearing output records, and outfmt 0/6/7 byte comparisons remain open. Natural positive postredo containment and composition heap replacement still need fixtures if reached. Do not treat this as Stage D or E completion, TBLASTN certification, or performance evidence.

Validation: [VALIDATION_KAPPA_RESULT_20260925.txt](VALIDATION_KAPPA_RESULT_20260925.txt).

# v0.1.0 implementation recertification — 2026-09-17

Implementation S: `0dd6c2b83f437a940d5fcbea5ca8a247934b48f3`.
Status: `INTEGRATED_RUNTIME_CERTIFIED` and `CROSS_PLATFORM_NATIVE_CERTIFIED`;
independent raw-evidence and archive reviews passed.
Publication and exact-candidate artifact readiness are separate gates.

## Authority and input identity

The user approved Gate A version 2 after NCBI source review established the
correct automatic megablast X-drop behavior. Only the Sakai canonical output
changed; the other 42 canonical rows and all Gate B platform fingerprints are
unchanged. BLASTN now has 14 `EXACT_TEXT` cases. The six TBLASTX local-subject
non-default genetic-code cases remain the sole intentional product deviation.
See [the authority review](../evidence/v010_distribution_20260917/AUTHORITY_REVIEW.md).

Both completed certification receipts bind S and certification-input SHA-256
`f8b70ffe832e266d1d2775ae8f408f550804c49b72fb37f19c3825eedbe4053f`.
The digest covers 417 Git entries and the existing release-contract semantics:
production/build inputs, fixtures, classifiers, runners, toolchain/config,
Product Decisions and certification workflows. Historical receipts are retained
in `v0.1.0_rc_contract.json`; they are not relabeled as current measurements.

## Completed gates

| Gate | Result | Evidence identity |
| --- | --- | --- |
| Linux integrated | 43 native contracts: BLASTN 14 exact, BLASTP 9 exact, TBLASTX 14 exact plus six approved deviations; 41 native/serial byte equalities; two BLASTP native thread pairs | Manifest `0e946e7313e28917dcd071e116c18d56586896ec7383ccfce5b74e80cd6598a6` |
| Integrated repeatability | Six representatives, native and serial Wasm, three completed runs per target; all raw hashes repeat | Included in integrated manifest |
| Windows x64, macOS arm64, macOS x64 | Each: 43 Gate A outputs, six registered Gate B fingerprints, 12 extra repeats; 183 executions total | [Run 35182319346](https://github.com/satoshikawato/LOSAT/actions/runs/35182319346); manifest `702c1644f10d84e6cb2bf60525537002ff39e0383ac0cf08a9d6a2a36c6454b3` |
| Ordinary S CI | Rust and threaded-Wasm jobs passed; 104 frozen outputs match, including the previously timed-out p11 case within the unchanged 3600-second deadline | [Run 35182287486](https://github.com/satoshikawato/LOSAT/actions/runs/35182287486); artifact manifest `1899b5707f4f91a21d07873b204ec23031c77c618cb290dd25e9fe0f9ea63a9a` |

Rust/Cargo is 1.92.0 throughout. Integrated serial execution uses Node 18.19.1;
threaded CI uses Node 24.21.0. Official NCBI BLAST+ 2.17.0 binaries and source
archive are validation oracles only; their hashes are retained in raw evidence.
The pure-Rust boundary checker passed with zero production delegation findings.

## Interrupted integrated run

The integrated process stopped after the full native/serial matrices and 14 of
24 extra repeats. The cause is not established. All 576 checkpoint files were
preserved and rehashed before recovery. Checkpoint manifest:
`203c7aa314b5f81e6168885b6513f574f7023138236852745da45f8480744995`.

An independently reviewed external adapter executed only the ten missing repeat
slots, with the original argv, explicit environment overrides, fixtures and
artifact identities. The original S certifier reclassified and aggregated all
151 logged search slots plus two retained default-code probe searches. The
adapter did not modify committed code or rerun completed searches. One interrupted
p11 serial run2 attempt was restarted; three *completed* runs, not three total
attempts, form each representative's sample. All original checkpoint files
remained unchanged. The ten-entry execution ledger records successful exits.

Historical command receipts did not capture inherited runtime environments;
full effective-environment equality across the interruption is not claimed.
Recovery records relevant launch environment. No performance claim is made.

## Durable evidence

The local evidence handoff is retained under
`artifacts/v010-distribution-20260917/` in the original LOSAT workspace. Archives
contain raw outputs, argv, fixture/oracle identities, toolchain metadata and
manifests; the integrated archive also contains the adapter and recovery ledger.
They must accompany review; paths alone are not portable evidence.

| Archive | SHA-256 |
| --- | --- |
| `integrated-complete-0dd6c2b8.tar.gz` | `5666f6f55529dbb9d332466a47a0c27ebb0b2cae7ecdbeb62b8be242d4949401` |
| `platform-complete-35182319346.tar.gz` | `3497d89a2a8ab14ae7b717fd5234dccf998dec47e993fcf262c14e1e7678d492` |
| `ci-35182287486.tar.gz` | `c3f1951b68e04caa11b622a9e1ccabc6981abfafc06f939aada3c61d625f131d` |

## Candidate and support limits

The final candidate R may add only provenance and documentation to S. The
release helper must verify identical bound inputs before assembling six exact-R
artifacts. R's CI, RC handoff, actual final binaries' runtime requirements and
minimum-environment smoke checks remain required. This record does not claim
publication, signing, notarization, Rosetta execution, older macOS execution,
generic BLAST compatibility or a performance improvement.

# BLASTP NCBI Parity 全面修正計画書

## 文書の位置づけ
この文書は 2026-03-17 時点の BLASTP parity 修正計画と実装結果を反映した進捗管理文書である。  
当初計画は維持するが、以下を追加で明記する。

- 完了済みの作業
- 作業中に新たに判明した課題
- 現時点の数値的な改善状況
- 次に着手すべき順序

## 目的
LOSAT の `blastp` 実装を NCBI BLAST+ と bit-perfect に一致させる。  
対象は検索本体だけでなく、`outfmt 0/6/7`、custom tabular fields、task 別既定値、統計値、整形出力まで含む。  
`pairwise` を含む全出力 parity は必須であり、部分 parity は受け入れない。

## 2026-03-17 最新状態
2026-03-17 時点で、`blastp` の今回の parity slice は `WSSV.faa` vs `PajaWSV.faa` と `PajaWSV.faa` vs `SicyWSV.faa` の `outfmt 6` で zero diff を維持している。  
ただし、その後に `run_blastp_comparison.sh` を広げると `WSSV.faa` vs `SicyWSV.faa` に別件の残差が 1 本残る。

最新確認:

- `cd LOSAT && cargo build --release --bin LOSAT`: 通過
- `cd LOSAT && /usr/bin/time -f '%e' ./target/release/LOSAT blastp -query tests/fasta/WSSV.faa -subject tests/fasta/PajaWSV.faa -outfmt 6 -evalue 1e-5 -out /tmp/WSSV.PajaWSV.release.out`: `2.43s`
- `cd LOSAT && diff -u tests/blast_out/blastp/WSSV.PajaWSV.blastp.out /tmp/WSSV.PajaWSV.release.out`: 差分 0
- `cd LOSAT && /usr/bin/time -f '%e' ./target/release/LOSAT blastp -query tests/fasta/PajaWSV.faa -subject tests/fasta/SicyWSV.faa -outfmt 6 -evalue 1e-5 -out /tmp/PajaWSV.SicyWSV.release.out`: `0.86s`
- `cd LOSAT && diff -u tests/blast_out/blastp/PajaWSV.SicyWSV.blastp.out /tmp/PajaWSV.SicyWSV.release.out`: 差分 0
- `cd LOSAT/tests && bash run_blastp_comparison.sh`: `WSSV.SicyWSV.blastp` で停止
- `cd LOSAT && diff -u tests/blast_out/blastp/WSSV.SicyWSV.blastp.out /tmp/WSSV.SicyWSV.release.final.out`: `YP_009220499.1 / BDT63161.1` の extra HSP 1 本が残る
- `cd LOSAT && /usr/bin/time -f '%e' blastp -query tests/fasta/WSSV.faa -subject tests/fasta/PajaWSV.faa -outfmt 6 -evalue 1e-5 -out /tmp/WSSV.PajaWSV.ncbi.out`: `1.19s`
- `cd LOSAT && /usr/bin/time -f '%e' blastp -query tests/fasta/PajaWSV.faa -subject tests/fasta/SicyWSV.faa -outfmt 6 -evalue 1e-5 -out /tmp/PajaWSV.SicyWSV.ncbi.out`: `0.32s`

この turn で確定したこと:

- `WSSV.PajaWSV.blastp` は 2026-03-17 時点で zero diff。2026-03-16 時点の failing case ではなくなった。
- `PajaWSV.SicyWSV.blastp` の残差 2 ペア `BDT63508.1/BDT63134.1` と `BDT63570.1/BDT63112.1` は zero diff になった。
- 効いた修正は NCBI 実装差分の除去だけである。後段補正や formatter 側の救済は追加していない。
- `LOSAT/src/algorithm/blastp/blast_engine.rs` では、NCBI `blast_hits.c:2455-2538` と `blast_engine.c:540-555` / `blast_kappa.c:3612-3619` に合わせて preliminary HSP の common-endpoint purge を Kappa 入力前に復元した。
- `LOSAT/src/algorithm/blastp/blast_engine.rs` では、NCBI `blast_parameters.c:457-463` と `blast_kappa.c:2387-2415` に合わせて protein gapped x-drop の bits-to-raw 変換を切り捨て式へ修正した。
- `LOSAT/src/algorithm/blastp/blast_engine.rs`、`LOSAT/src/algorithm/blastp/hsp.rs`、`LOSAT/src/algorithm/blastp/kappa.rs`、`LOSAT/src/core/composition_adjustment/redo_alignment.rs` では、NCBI の `qsort` 順を再現するため stable sort をやめて `sort_unstable_by` に置換した。
- `LOSAT/src/stats/spouge.rs` と `LOSAT/src/algorithm/blastp/blast_engine.rs` では、NCBI `blast_stat.c:5236-5263` と `blast_parameters.c:932-940` に合わせて `BLAST_SpougeEtoS` ベースの prelim cutoff 計算を追加したが、`WSSV.SicyWSV` の extra HSP はまだ残っている。
- `LOSAT/src/algorithm/blastp/args.rs` では、NCBI `blast_advprot_options.cpp:64-67` に従い、`CompositionMatrixAdjust` 既定時の `-seg` は `No` のまま維持した。`SetSegFiltering(true)` へ寄せる変更は oracle と食い違うため採用しない。
- 診断専用に追加していた ignored test と単発デバッグ補助は削除した。常設コードに残したのは NCBI port 本体だけである。

今回の slice に関する残 blocker:

- `run_blastp_comparison.sh` はまだ zero diff ではない。現時点の blocker は `WSSV.SicyWSV.blastp` の `YP_009220499.1 / BDT63161.1 22.137 393 228 14 18 363 200 561 2.47e-06 42.7` の extra HSP 1 本である。
- 次は、この HSP が NCBI では full search で落ちる段階を `blast_parameters.c` / `blast_engine.c` / gapped prelim path まで戻って固定し、その stage だけを NCBI へ置換する。

## 2026-03-18 追記
2026-03-18 に、2026-03-17 時点で blastp Kappa/redo 周辺にまだ残っていた NCBI 差分を 2 件追加で除去した。  
ただし、この 2 件は正しい NCBI port ではあるが、`AP027131.faa` vs `NZ_CP006932.faa` の残差 2 HSP 自体はまだ不変である。

### この日に除去した NCBI 差分

#### 1. SEG overlap merge の欠落
完了。

実施内容:

- `LOSAT/src/utils/seg.rs` で、NCBI `blast_filter.c:1142-1154` と `blast_seg.c:2125-2149` / `2300-2319` に従い、`sparamsp->overlaps = TRUE` 時の `s_MergeSegs` 相当 merge を追加した。
- `LOSAT/src/core/blast_seg.rs` の duplicate 実装は使わず、`utils::seg` を唯一の SEG 実装として維持した。

意味:

- これは subject SEG masking の常時条件そのものであり、後段 suppression ではない。
- NCBI では merge される overlapping mask interval を LOSAT が別々に残していた差分を削除した。

#### 2. `subject_maybe_biased` carry-over と window 内 alignment sort の欠落
完了。

実施内容:

- `LOSAT/src/algorithm/blastp/kappa.rs` で、NCBI `blast_kappa.c:1626-1636` に従い、`build_subject_range_data` が毎回 `subject_maybe_biased = TRUE` から始める処理を削除した。
- これにより、NCBI と同様に「一度 biased でないと判明した subject には再度 SEG をかけない」 pointer semantics を復元した。
- `LOSAT/src/core/composition_adjustment/redo_alignment.rs` で、NCBI `redo_alignment.c:738-747` に従い、`windows_from_protein_aligns` が各 window の `align` に `s_DistinctAlignmentsSort` をかける処理を復元した。
- これにより、redo loop に入る HSP 順序と `window->align` 先頭依存の near-identical / SEG 判定順を NCBI と同じ順序へ戻した。

意味:

- どちらも Kappa redo 層の処理順そのものの差であり、formatter や後段 filter の調整ではない。
- ただし、この 2 差分を消しても `AP027131/NZ_CP006932` の 2 HSP 残差は変化しなかった。

### 2026-03-18 の再確認結果

実行コマンド:

```bash
cd LOSAT && cargo build --release --bin LOSAT
cd LOSAT && ./target/release/LOSAT blastp -query tests/fasta/WSSV.faa -subject tests/fasta/SicyWSV.faa -outfmt 6 -evalue 1e-5 -out /tmp/WSSV.SicyWSV.release.out
cd LOSAT && diff -u tests/blast_out/blastp/WSSV.SicyWSV.blastp.out /tmp/WSSV.SicyWSV.release.out
cd LOSAT && ./target/release/LOSAT blastp -query tests/fasta/AP027131.faa -subject tests/fasta/NZ_CP006932.faa -outfmt 6 -evalue 1e-5 -out /tmp/AP027131.current.out
cd LOSAT && diff -u tests/blast_out/blastp/AP027131.NZ_CP006932.blastp.out /tmp/AP027131.current.out
```

結果:

- `cargo build --release --bin LOSAT`: 通過
- `WSSV.faa` vs `SicyWSV.faa`: zero diff 維持
- `AP027131.faa` vs `NZ_CP006932.faa`: まだ 2 HSP 不一致

残差の内容:

- `BDV02435.1 / WP_025208655.1`
  - NCBI: `1719-2383 / 33-590`, `bit score 129`
  - LOSAT: `1793-2383 / 33-590`, `bit score 108`
- `BDV02435.1 / WP_025208654.1`
  - NCBI にのみ `2192-2380 / 48-241`, `bit score 60.8` が存在

### 現在の判断

- heap/Kappa result acceptance 側の明白な NCBI 差分は、2026-03-18 時点でさらに 2 件除去済みである。
- それでも残差が不変である以上、次の主犯は preliminary gapped admission か full traceback alignment 本体である。
- 優先順位は以下に更新する。

1. `blast_gapalign.c` の `ALIGN_EX` / `Blast_SemiGappedAlign` / `s_RestrictedGappedAlign` を Rust 実装と一対一で再照合し、start/stop 境界と traceback script 生成の差分を消す。
2. それでも不変なら `blast_parameters.c:319-373` と `blast_gapalign.c:3880-4010` の cutoff/admission 条件をもう一段前で置換する。
3. formatter 側や後段の suppression は引き続き禁止する。

### 2026-03-18 再追記
`blast_gapalign.c:658-665` に合わせて `ALIGN_EX` の traceback row が未使用セルを露出しないよう修正し、再度 `cargo build --release --bin LOSAT` と最終比較を実行した。  
結果は以下で、`AP027131/NZ_CP006932` の 2 HSP 残差は依然として不変だった。

- `WSSV.faa` vs `SicyWSV.faa`: zero diff 維持
- `AP027131.faa` vs `NZ_CP006932.faa`: 残差 2 HSP のまま不変

この確認で確定したこと:

- `ALIGN_EX` row allocator の未使用セル露出は、NCBI 差分としては修正済みだが、現残差の主犯ではなかった。
- `blast_gapalign.c` の行 DP / traceback 本体だけを直しても不変である以上、次の直接比較対象は `aa_ungapped.c` 側の ungapped HSP 生成と、その HSP が `BlastGetStartForGappedAlignment` / preliminary gapped path に渡るまでの入力である。
- 特に `BDV02435.1 / WP_025208655.1` の残差は、より長い NCBI 側 HSP が生成されず、短い代替 HSP だけが残っている形なので、後段 suppression ではなく upstream HSP input の差を疑うべき段階に入った。

次の優先順位は以下へさらに更新する。

1. `aa_ungapped.c` と `LOSAT/src/algorithm/blastp/extension.rs`、および `InitHSP -> UngappedHit` 変換を一対一で照合し、`q_start/s_start/gapped_start` 入力差分を除去する。
2. そのうえで `blast_gapalign.c:3393-4010` の `BlastGetStartForGappedAlignment` と preliminary gapped admission を再確認する。
3. ここまで一致してなお残る場合に限って、`redo_alignment.c` / `blast_kappa.c` の incoming alignment distinctness を再点検する。

### 2026-03-18 source 照合追記
2026-03-18 の追加照合では、`AP027131/NZ_CP006932` の残差 2 HSP を対象に、NCBI source と LOSAT source を結果比較ではなく実装比較でさらに前段まで戻って突き合わせた。  
この時点では新しい parity fix はまだ入れていないが、どこが主犯でないかをかなり狭められた。

この照合で NCBI 差分ではないと確認できた層:

- `aa_ungapped.c:804-1177` と `LOSAT/src/algorithm/blastp/extension.rs`
  - `s_BlastAaExtendRight`
  - `s_BlastAaExtendLeft`
  - `s_BlastAaExtendOneHit`
  - `s_BlastAaExtendTwoHit`
- `blast_extend.c:274-375` と `LOSAT/src/algorithm/tblastx/blast_gapalign.rs`
  - `BlastSaveInitHsp`
  - `score_compare_match`
  - `InitHSP -> UngappedHit` の half-open 変換
- `blast_gapalign.c:3394-3438` と `LOSAT/src/algorithm/blastp/gapalign.rs`
  - `BlastGetStartForGappedAlignment`
- `blast_gapalign.c:4163-4319` と `LOSAT/src/algorithm/blastp/gapalign.rs`
  - `AdjustSubjectRange`
  - score-only `Blast_SemiGappedAlign`
  - score-only `s_RestrictedGappedAlign`
  - preliminary protein gapped start/stop の復元
- `blast_kappa.c:936-1329` と `LOSAT/src/core/composition_adjustment/redo_alignment.rs`
  - `s_ExtendRight`
  - `s_ExtendLeft`
  - `s_FindNumIdentical`
  - `s_TestNearIdentical`
- `blast_kappa.c:1573-1704` と `LOSAT/src/algorithm/blastp/kappa.rs`
  - `s_SequenceGetProteinRange`
  - query/subject range 切り出し
  - SEG 適用条件
- `blast_gapalign.c:3485-3658` と `LOSAT/src/algorithm/blastp/blast_engine.rs`
  - `s_CompareInitHSPsByQueryOffsetScore`
  - `s_ChainingAlignment`

この照合で残った有力 suspect:

- `blast_itree.c` の preliminary containment / common-endpoint admission
- preliminary gapped path に入る前の HSP admission 順
- upstream で long seed/HSP が落ち、shorter replacement HSP だけが残る経路

この時点の判断:

- `BDV02435.1 / WP_025208655.1` の `1719-2383 / 33-590` が LOSAT では `1793-2383 / 33-590` に短縮される主因は、`redo_alignment` や formatter ではなく、preliminary containment 以前の HSP admission 側にある可能性が高い。
- `BDV02435.1 / WP_025208654.1` の missing 1 本も、同じ upstream admission 差分で説明できる余地が大きい。

次の優先順位は以下へ更新する。

1. `blast_itree.c` と `LOSAT/src/algorithm/blastn/interval_tree.rs` を blastp preliminary path の使い方込みで一対一照合し、`BlastIntervalTreeContainsHSP` / common-endpoint removal の差分を除去する。
2. その後に `blast_gapalign.c:3870-4010` の preliminary HSP admission 順と `Blast_HSPListSaveHSP` / interval-tree rebuild のタイミングを再確認する。
3. ここまで一致してなお残る場合に限って、ungapped hit ordering と `lookup -> InitHSP` 生成順そのものへ戻る。

## 2026-03-19 追記
※ この節の `AP027131.faa` vs `NZ_CP006932.faa` 残差 `2 HSP` という観測は、同日後段の再比較で superseded になっている。  
現在の authoritative な current state は、本書末尾の `2026-03-19 再追記: blastp upstream HSP admission parity slice` を参照すること。

2026-03-19 には、2026-03-19 parity slice として planned だった `blastp` preliminary gapped admission / interval-tree containment / Kappa handoff の NCBI 差分除去を実装し、helper unit test と最終比較を実行した。  
結果として、unordered storage と interval-tree sign 判定の差分は除去できたが、`AP027131.faa` vs `NZ_CP006932.faa` の残差 2 HSP はまだ不変である。  
従って、この slice は NCBI 差分の除去としては正しいが、現 blocker の主犯ではなかった。

### この日に除去した NCBI 差分

#### 1. preliminary HSP / Kappa handoff の unordered storage
完了。

実施内容:

- `LOSAT/src/algorithm/blastp/blast_engine.rs` で、subject ごとの preliminary HSP を 1 本の ordered `Vec<BlastpPreliminaryHsp>` として保持し、retry もこの vector を唯一の truth として扱うよう修正した。
- `restricted -> exact` retry では、NCBI `blast_gapalign.c:3975-4007` に合わせて current query の HSP だけを ordered vector から除去し、残りの HSP から interval tree を rebuild する経路へ置換した。
- subject 走査後の query 分割は `HashMap` をやめ、query index 昇順の `Vec<Vec<BlastpPreliminaryHsp>>` へ置換した。
- Kappa 手前の surviving preliminary 保持は `Vec<Vec<(u32, Vec<BlastpPreliminaryHsp>)>>` に置換し、sorted preliminary hitlist の `oid` を使った線形 lookup で取り出すようにした。
- これにより、`Blast_HitListUpdate` への入力順と Kappa 読み出し順のあいだに Rust 側の unordered iteration が入らない形になった。

意味:

- これは NCBI `blast_gapalign.c` / `blast_hspstream.c` / `blast_kappa.c` の処理順そのものの復元であり、後段補正ではない。
- `HashMap` 依存の subject/query 順 drift はこの時点で除去済みである。

#### 2. interval-tree containment の subject frame sign 判定 drift
完了。

実施内容:

- `LOSAT/src/algorithm/blastn/interval_tree.rs` で、containment 判定 `s_HSPIsContained` 相当が `subject.frame` の完全一致を見ていた差分を削除し、NCBI `blast_itree.c:819-843` に合わせて `SIGN(subject.frame)` 比較へ修正した。
- あわせて、common-endpoint winner が equal score 時に shorter HSP を優先することと、containment が subject frame の符号だけで比較されることを unit test で固定した。

意味:

- これは `BlastIntervalTreeContainsHSP` の NCBI 差分そのものであり、blastp preliminary containment に直接効く層である。
- ただし、この drift を除去しても現 residual case の 2 HSP は不変だった。

### 2026-03-19 の再確認結果

実行コマンド:

```bash
cargo test --manifest-path LOSAT/Cargo.toml --lib blastp::blast_engine -- --nocapture
cargo test --manifest-path LOSAT/Cargo.toml --lib blastn::interval_tree -- --nocapture
cargo build --manifest-path LOSAT/Cargo.toml --release --bin LOSAT
cd LOSAT && ./target/release/LOSAT blastp -query tests/fasta/AP027131.faa -subject tests/fasta/NZ_CP006932.faa -outfmt 6 -evalue 1e-5 -out /tmp/AP027131.NZ_CP006932.release.out
cd LOSAT && diff -u tests/blast_out/blastp/AP027131.NZ_CP006932.blastp.out /tmp/AP027131.NZ_CP006932.release.out
```

結果:

- `cargo test --manifest-path LOSAT/Cargo.toml --lib blastp::blast_engine -- --nocapture`: 16 件通過
- `cargo test --manifest-path LOSAT/Cargo.toml --lib blastn::interval_tree -- --nocapture`: 6 件通過
- `cargo build --manifest-path LOSAT/Cargo.toml --release --bin LOSAT`: 通過
- `AP027131.faa` vs `NZ_CP006932.faa`: まだ 2 HSP 不一致
- gating diff が nonzero のため、`cd LOSAT/tests && bash run_blastp_comparison.sh` は未実行

残差の内容:

- `BDV02435.1 / WP_025208655.1`
  - NCBI: `1719-2383 / 33-590`, `bit score 129`
  - LOSAT: `1793-2383 / 33-590`, `bit score 108`
- `BDV02435.1 / WP_025208654.1`
  - NCBI にのみ `2192-2380 / 48-241`, `bit score 60.8` が存在

### 現在の判断

- preliminary HSP の ordered storage / Kappa handoff 順 / interval-tree sign 判定の NCBI 差分は 2026-03-19 時点で除去済みである。
- それでも residual が不変である以上、主犯は Kappa handoff の subject 順や interval-tree sign 判定ではない。
- 次の直接対象は、引き続き upstream の preliminary admission 本体である。

次の優先順位は以下へ更新する。

1. `blast_gapalign.c:3870-4010` の preliminary HSP admission 順と `Blast_HSPListSaveHSP` / tree update timing を、`InitHSP -> preliminary HSP` 入力順まで含めて再照合する。
2. それでも不変なら `aa_ungapped.c` と `lookup -> InitHSP` 生成順へ戻り、`BDV02435.1` の longer HSP がどの段階で失われるかを source comparison だけで特定する。
3. formatter 側、後段 suppression、oracle 側比較に依存した弥縫策は引き続き禁止する。

## 基本方針
- NCBI BLAST C/C++ source のみを唯一の仕様とする。
- NCBI source に存在しない挙動は実装しない。
- NCBI executable は comparison oracle としてのみ使用する。
- LOSAT runtime、fallback、feature implementation に NCBI 実装を使わない。
- 全コード変更に NCBI の file path と line number を含む参照コメントを付与する。
- `pairwise` を後回しにしない。`outfmt 0/6/7` はすべて parity 対象とする。

## 対象範囲
### 含むもの
- `blastp`
- `blastp-short`
- `blastp-fast`
- `outfmt 0`
- `outfmt 6`
- `outfmt 7`
- custom tabular fields
- `--comp_based_stats`
- `--seg`
- `--ungapped`
- `-use_sw_tback`
- NCBI table に存在する matrix/gap 組み合わせ
- word size `2/3/5/6/7`

### 含まないもの
- PSI-BLAST
- PHI-BLASTP
- DELTA-BLAST
- KBLASTP

これらは別機能として扱い、本計画の対象外とする。

## 2026-03-15 時点の現状
作業開始時点では、再ビルド済み `target/debug/LOSAT` に対する `WSSV.faa` vs `PajaWSV.faa` の比較で少なくとも以下の divergence があった。

- `outfmt 6`: 参照 178 行に対して LOSAT 1224 行
- `outfmt 0`: 参照 9530 行に対して LOSAT 14196 行

2026-03-15 の実装修正後、同じ比較で以下まで改善した。

- `outfmt 6`: `1224 -> 1169 -> 912 -> 829 -> 281 -> 271 -> 218`
- `outfmt 0`: `14196 -> 13382 -> 8734`

さらに同日中に、blastp ローカル traceback-backed gapped path と
post-traceback lifecycle を追加実装し、`WSSV.faa` vs `PajaWSV.faa` の
`outfmt 6` を再測定した。結果は `829` 行のままで据え置きだった。

その後、初回 gapped pass の score-only 化、query 単位の
restricted/exact 再始動、preliminary HSP 入力の `kappa.rs` 接続まで追加実装し、
以下を再確認した。

- `cargo build --manifest-path LOSAT/Cargo.toml`: 通過
- `cargo test --manifest-path LOSAT/Cargo.toml --lib blastp::kappa -- --nocapture`: 4 件通過
- `WSSV.faa` vs `PajaWSV.faa` の `outfmt 6`: 参照 178 行に対して LOSAT 829 行のまま

さらに同日中に、`redo_alignment.rs` の non-SW protein windowing /
`Blast_RedoOneMatch` 制御フローと、`kappa.rs` の arena-backed redo adapter を追加実装し、
以下を確認した。

- `cargo build --manifest-path LOSAT/Cargo.toml`: 通過
- `cd LOSAT/tests && bash run_blastp_comparison.sh`: 2026-03-15 実行、最初の failing case は `WSSV.PajaWSV.blastp`
- `WSSV.faa` vs `PajaWSV.faa` の最新 `outfmt 6`: 参照 178 行に対して LOSAT 271 行
- full fixture sweep は最初の failing case で停止したため未完了

さらに同日中に、protein gapped x-drop の bits-to-raw 変換を
`blast_parameters.c` / `blast_kappa.c` に合わせ、`kappa.rs` 側の
composition-adjusted E-value scaling と postprocess 順を `blast_kappa.c`
へ寄せて再測定した。結果は以下である。

- `cargo build --manifest-path LOSAT/Cargo.toml`: 通過
- `cargo test --manifest-path LOSAT/Cargo.toml --lib blastp::kappa -- --nocapture`: 7 件通過
- `cargo test --manifest-path LOSAT/Cargo.toml --lib core::composition_adjustment -- --nocapture`: 15 件通過
- `WSSV.faa` vs `PajaWSV.faa` の最新 `outfmt 6`: 参照 178 行に対して LOSAT 218 行
- `cd LOSAT/tests && bash run_blastp_comparison.sh`: 最初の failing case はまだ `WSSV.PajaWSV.blastp` のままだが、同ケースの diff は縮小

重要な点:

- `outfmt 6` は `829 -> 281 -> 271 -> 218` まで低下し、redo/kappa 接続、
  `composition_adjustment` slice 1 導線追加、protein x-drop raw-score parity の順で
  改善が積み上がっている。
- ただし、最新 slice 後も `outfmt 6` はまだ 178 行に対して 218 行で、過剰 hit は残っている。
- `outfmt 0` は行数だけ見ると参照より少なくなったが、これは parity 達成ではない。pairwise formatter がまだ citation block、database text、alignment body、footer などを正しく再現できていないため、行数の一致不一致だけでは評価できない。
- `outfmt 0` の version header は `BLASTP 2.17.0+` に修正済みで、以前の `BLASTP 0.1.0` という明確な divergence は解消した。
- `run_blastp_comparison.sh` は 2026-03-15 に再実行し、最初の failing case が
  `WSSV.PajaWSV.blastp` であることを確認した。全 fixture sweep と `outfmt 0/7`
  の再測定はまだ行っていない。
- traceback/lifecycle の port 後と score-only 初回 gapped pass / restart 導入後は
  829 行で据え置きだったが、non-SW redo/kappa 接続で 281 行、
  `adjust_scores.rs` 導線追加後に 271 行まで下がった。
- `WSSV.PajaWSV.blastp` の先頭差分は当初 1 行目の coordinates 差分が前面だったが、
  最新 slice 後は top-hit ordering を維持したまま long HSP boundary が参照へ近づき、
  残差は `coordinates` に加えて `score/evalue` も明確に残る段階へ移った。
- `LOSAT/tests/run_blastp_outfmt7_comparison.sh` と
  `LOSAT/tests/run_blastp_custom_fields_comparison.sh` は存在するが、
  `LOSAT/tests/blast_out/blastp_outfmt7` と
  `LOSAT/tests/blast_out/blastp_custom_fields` はまだ未作成である。
- 最新 slice 後も 178 行との差は大きいため、現在の主残差は formatter よりも
  `protein gapped score/boundary parity + reap timing + e-value adjustment` にあり、
  その後段として full `Blast_AdjustScores` / Smith-Waterman / unified P 未移植側が残っている。

## 2026-03-16 追記
2026-03-16 には `Blast_AdjustScores` と protein traceback の 2 slice を進め、`WSSV.faa` vs `PajaWSV.faa` の `outfmt 6` / `outfmt 0` を再測定した。

### この日に完了した差分除去

#### 1. `Blast_AdjustScores` の NCBI matrix-adjust path 置換
完了。

実施内容:

- `LOSAT/src/core/composition_adjustment/adjust_scores.rs` の既存 fallback 主導経路を削除し、
  `composition_adjustment.c` / `optimize_target_freq.c` / `nlm_linear_algebra.c`
  に基づく `CompositionMatrixAdjust` 経路へ置換した。
- `Blast_AdjustScores` は `matrix_adjust_rule` を見て nonfatal fallback に逃がすのではなく、
  まず adjusted matrix 生成を試みる NCBI 順へ合わせた。
- `blast_get_relative_entropy`、`set_xuo_scores`、Newton 系線形代数、
  `optimize_target_frequencies`、`composition_matrix_adjust` を Rust 側へ追加した。

観測された改善:

- `WSSV.faa` vs `PajaWSV.faa` の `outfmt 6` は `218 -> 174` 行まで減少した。
- top hit の coordinates は NCBI に一致した。
- ただしこの時点では top hit の `%identity` / `mismatch` / `positives` が大きく食い違っており、
  score parity ではなく traceback/edit-script parity が次の主残差だと判明した。

#### 2. `ALIGN_EX` forward prelim traceback の二重反転除去
完了。

実施内容:

- `LOSAT/src/algorithm/blastp/gapalign.rs` の `align_ex_protein` で、
  forward half の preliminary traceback を `ALIGN_EX` 内で forward 化していた処理を削除した。
- NCBI `blast_gapalign.c` に合わせて、`ALIGN_EX` は `GapPrelimEditBlockAdd` の格納順をそのまま返し、
  `Blast_PrelimEditBlockToGapEditScript` 側だけが `fwd_prelim_tback` を reverse する形に揃えた。
- あわせて `blast_traceback.c` / `blast_kappa.c` の `query_nomask` 導線に合わせ、
  pairwise 構築と postprocess の identities 再計算で `aa_seq_nomask` を参照する helper を追加した。

観測された改善:

- `outfmt 6` の行数は `174` のままだが、top hit の `%identity` / `mismatch` / `gapopen` /
  pairwise body が NCBI と一致する段階まで戻った。
- `outfmt 0` の first HSP は以下まで一致した。
  - `Score = 599 bits (1542)` までは LOSAT も同じ raw score を出す。
  - `Identities = 450/1148 (39%), Positives = 601/1148 (52%), Gaps = 157/1148 (14%)`
    は NCBI と一致した。

### 2026-03-16 時点の最新測定

実行コマンド:

```bash
cargo test --manifest-path LOSAT/Cargo.toml --lib blastp::gapalign -- --nocapture
cargo test --manifest-path LOSAT/Cargo.toml --lib blastp::kappa -- --nocapture
cargo build --manifest-path LOSAT/Cargo.toml
LOSAT/target/debug/LOSAT blastp -query LOSAT/tests/fasta/WSSV.faa -subject LOSAT/tests/fasta/PajaWSV.faa -outfmt 6 -evalue 1e-5 -max_target_seqs 10 -out /tmp/WSSV.PajaWSV.after_tracefix.out
LOSAT/target/debug/LOSAT blastp -query LOSAT/tests/fasta/WSSV.faa -subject LOSAT/tests/fasta/PajaWSV.faa -evalue 1e-5 --max-target-seqs 1 --max-hsps-per-subject 1 --outfmt 0 -out /tmp/losat_blastp_pairwise_after_tracefix.txt
```

結果:

- `cargo test --manifest-path LOSAT/Cargo.toml --lib blastp::gapalign -- --nocapture`: 9 件通過
- `cargo test --manifest-path LOSAT/Cargo.toml --lib blastp::kappa -- --nocapture`: 11 件通過
- `cargo build --manifest-path LOSAT/Cargo.toml`: 通過
- `WSSV.faa` vs `PajaWSV.faa` の `outfmt 6`: 参照 178 行に対して LOSAT 174 行
- top line の差分:
  - coordinates / `%identity` / `alignment length` / `mismatch` / `gapopen` は一致
  - LOSAT: `6.79e-173`, `599`
  - NCBI: `0.0`, `598`

### 2026-03-16 時点でまだ未一致の NCBI 関数
この日の slice で残差は traceback から e-value / bit-score / prune timing 側へ移った。現時点で未一致として次に詰める対象は以下である。

- `blast_kappa.c`
  - `s_HSPListNormalizeScores`
  - `s_AdjustEvaluesForComposition`
  - `s_HitlistEvaluateAndPurge`
  - `Blast_HSPListReapByEvalue`
- `composition_adjustment.c`
  - `Blast_AdjustScores` の default path は入ったが、
    `lambda_ratio` / `pvalueForThisPair` / adjusted score normalization の最終 bit parity は未完了
- `blast_hits.c`
  - reevaluate 後の `best_evalue` / heap replacement / final prune timing は
    `Blast_HitListUpdate` とまだ完全一致していない可能性が高い

### 次の着手順

1. `s_HSPListNormalizeScores` と `s_AdjustEvaluesForComposition` の raw-score 正規化順、
   `LambdaRatio`、`pvalueForThisPair` の流れを `blast_kappa.c` に揃える。
2. `s_HitlistEvaluateAndPurge` / `Blast_HSPListReapByEvalue` / `Blast_HitListUpdate`
   の timing を固定し、`174 -> 178` の hit-count 差を消す。
3. `WSSV.PajaWSV.blastp` が zero diff になった時点で `run_blastp_comparison.sh`
   全 fixture へ広げる。

## 2026-03-15 に完了した作業

### 1. 比較基盤の固定
完了。

実施内容:

- `tests/run_blastp_comparison.sh`
- `tests/run_blastp_pairwise_comparison.sh`

の両方で、出力先を tracked `losat_out/` ではなく temp directory に変更した。  
これにより、過去に生成された stale artifact が current source の parity を偽装する問題を除去した。

効果:

- 「tracked output は一致しているが current binary は一致していない」という状態を排除した。
- BLASTP oracle comparison の結果を、その時点の binary に対してのみ評価できるようになった。

### 2. Options / task 解決の parity
部分完了。

完了した内容:

- `blastp` / `blastp-short` / `blastp-fast` の task surface を Rust 側で解決可能にした。
- `blastp-short` の task default を NCBI に合わせた。
  - matrix `PAM30`
  - gap open `9`
  - gap extend `1`
  - evalue `20000`
  - word size `2`
  - SEG off
- `blastp-fast` の task default を NCBI に合わせた。
  - word size `5`
  - compressed lookup type
  - threshold `19.3`
  - chaining on
- `--ungapped`
- `-use_sw_tback`

を CLI surface として追加した。

- `--ungapped` と composition-based stats の組み合わせを NCBI と同じ条件で reject するようにした。

未完了:

- `blastp-fast` の runtime 実装は未完成で、compressed lookup と chaining はまだ execution parity になっていない。
- `--ungapped` と `-use_sw_tback` は surface parity までは進んだが、runtime path は未 port のため明示的に unsupported のままである。

### 3. lookup / cutoff / score 計算の初期補正
部分完了。

完了した内容:

- final HSP の bit score / e-value 計算を、lookup 用 ungapped Karlin params ではなく gapped Karlin params に変更した。
- initial HSP 保存 cutoff を、`args.evalue` と gapped search space から得る `cutoff_score_max` と、ungapped gap-trigger の最小値で決める形に寄せた。
- これにより、以前の score inflation と過剰通過の一部を是正した。

観測された改善:

- `outfmt 6` の先頭 hit の bitscore は大きく低下し、参照方向へ近づいた。
- ただし、まだ reference より高いスコアを持つ HSP が多数残る。

### 4. Gapped extension 開始点の補正
部分完了。

完了した内容:

- `BlastGetStartForGappedAlignment` 相当の開始点選択を追加した。
- これまでの実装は ungapped HSP 全体を seed として `extend_gapped_protein` に渡していたが、NCBI は HSP 中の最高スコア window 中央から gapped extension を開始する。
- この差分を修正した。

観測された改善:

- `outfmt 6` は `1169 -> 912` まで低下した。
- gapped path の過剰延長が一定量抑制された。

### 5. Interval tree containment の導入
部分完了。

完了した内容:

- gapped extension 実行前に NCBI と同様の interval-tree containment check を入れた。
- extension 済み HSP は tree に追加し、後続の ungapped HSP が既存 gapped HSP に包含される場合は skip するようにした。

観測された改善:

- `outfmt 6` は `912 -> 829` まで低下した。
- duplicated / contained HSP の一部が事前に落ちるようになった。

### 6. Pairwise header の NCBI version 反映
完了。

完了した内容:

- pairwise output の version を `2.17.0+` に合わせた。

改善点:

- 以前は `BLASTP 0.1.0` が出ていたため、header 先頭で即 divergence だった。
- 現在は少なくとも version line は NCBI 参照に一致する。

未完了:

- citation block
- database line の句読点・桁区切り
- summary table
- alignment body
- footer

は未 parity。

### 7. BLASTP 専用 hitlist / comparator / tabular path の分離
部分完了。

完了した内容:

- `blastp` 用の `BlastpHsp` / `BlastpHspList` / `BlastpHitList` 相当層を追加し、`BlastnHsp` / `BlastnHitList` 流用を blastp engine から外した。
- `GetPrelimHitlistSize`、`ScoreCompareHSPs`、`s_EvalueCompareHSPLists`、`Blast_HitListUpdate`、`Blast_TrimHSPListByMaxHsps` に対応する blastp 専用 comparator / hitlist 更新経路を追加した。
- `Blast_HSPListPurgeHSPsWithCommonEndpoints(..., TRUE)` に相当する purge を blastp path に導入した。
- `outfmt 6` / `outfmt 7` / custom tabular fields の標準経路を generic writer から切り離し、blastp 専用 tabular writer に一本化した。
- tabular 出力は custom fields だけでなく standard fields でも `PairwiseHit` 構築済みの同一 HSP truth を参照するようにした。
- `cargo build` は通過した。

未完了:

- preliminary evalue reap と final hitlist 更新の timing はまだ NCBI と照合途中である。
- 新しい blastp 専用 hitlist / tabular pathに対する comparison fixture の再測定は未実施で、zero-diff には到達していない。

### 8. BLASTP 専用 gapped path の分離と post-traceback lifecycle
部分完了。

完了した内容:

- `blastp/gapalign.rs` を追加し、`BlastGetStartForGappedAlignment`、
  `AdjustSubjectRange`、protein 向け `ALIGN_EX`、
  `BLAST_GappedAlignmentWithTraceback` 相当を blastp ローカル実装として導入した。
- blastp の gapped extension は `tblastx` 由来の `extend_gapped_protein` 依存から外し、
  traceback-backed な edit script 生成へ切り替えた。
- 最終 HSP は `gap_info` / edit script を保持し、`identities / positives / mismatches /
  gap opens / alignment length` を traceback 起点で再計算するようにした。
- traceback 後の HSP lifecycle として、
  `Blast_HSPListPurgeHSPsWithCommonEndpoints(..., FALSE)` 相当、
  trimmed fragment 再評価、null purge、score 再 sort、containment purge を
  hitlist update 前に行う経路を blastp path に導入した。
- 初回 gapped extension を final `Hit` 直生成から切り離し、
  `BlastpPreliminaryHsp` と score-only `s_BlastProtGappedAlignment` 相当の経路へ分離した。
- 初回 gapped pass の left/right extension に
  exact `Blast_SemiGappedAlign` 相当と restricted `s_RestrictedGappedAlign` 相当を追加した。
- `blast_engine.rs` に query 単位の `restricted_align_array`、
  `redo_index` / `redo_query`、query-local HSP eviction、interval-tree rebuild を導入し、
  restricted 結果が曖昧な query だけ exact へ切り替えて再始動する経路を追加した。
- subject/query hitlist への確定 HSP 挿入は、preliminary HSP 群の postprocess 後にだけ行うようにした。
- `cargo build --manifest-path LOSAT/Cargo.toml` は通過した。
- `cargo test --manifest-path LOSAT/Cargo.toml --lib blastp::kappa -- --nocapture` は 4 件通過した。
- `WSSV.faa` vs `PajaWSV.faa` の `outfmt 6` を temp output harness で再実行し、
  現状値が 829 行であることを確認した。

未完了:

- restricted/exact 再始動の大枠は入ったが、trigger hit の再走査位置、
  boundary、score、tree 更新タイミングの parity は fixture 差分上まだ未証明である。
- 初回 gapped pass は score-only に分離されたが、`Blast_RedoOneMatch` 以降の full redo path が未 port のため、
  この preliminary HSP truth を NCBI と同じ final truth へまだ収束させられていない。
- restricted/exact の左右 extension、`AdjustSubjectRange` clipping、
  query 単位 restart の unit test はまだ追加していない。
- integration 側の全 fixture sweep は未実施である。

### 9. non-SW `Blast_RedoOneMatch` / protein windowing port
部分完了。

完了した内容:

- `redo_alignment.rs` に `BlastCompo_GappingParams`、`BlastCompo_SequenceRange`、
  `BlastCompo_SequenceData`、`BlastCompo_MatchingSequence`、
  `BlastCompo_QueryInfo`、`WindowInfo`、`Blast_RedoAlignParams`、
  callback 群を追加し、protein / non-SW 専用の core 入口を作成した。
- `redo_alignment.c` 由来の `s_AlignmentsRev`、`s_AlignmentCopy`、
  `s_IsContained`、`s_LocationCompareWindows`、`s_SubjectCompareWindows`、
  `s_WindowsFromProteinAligns`、`s_preliminaryTestNearIdentical`、
  `Blast_RedoOneMatch` の non-SW protein branch を Rust 側へ移植した。
- `kappa.rs` を preliminary HSP arena と redone `Hit` arena を使う adapter に置き換え、
  `traceback_preliminary_blastp_hsp` を redo callback 経由で呼ぶ経路へ差し替えた。
- `hits_from_distinct_alignments` を追加し、distinct alignment から final `Hit` を再構成した後、
  contained-hit reap、e-value purge、normalize、identity 再計算を既存 helper で再利用する形に揃えた。
- `blast_engine.rs` は per-query grouping と restricted/exact restart を維持したまま、
  `postprocess_preliminary_hits` の入口だけ新 redo path に差し替えた。
- `redo_alignment.rs` と `kappa.rs` に、window sort / containment / distinct-end /
  arena round-trip / score-coordinate 保持の unit test を追加した。
- `BlastCompo_QueryInfo` 相当の Rust struct を拡張し、query composition と
  query word hash を保持できるようにした。
- `blast_kappa.c` 由来の `s_ExtendRight`、`s_ExtendLeft`、`s_GetHash`、
  `s_FindNumIdentical`、exact `s_TestNearIdentical` を Rust 側へ移植した。
- `kappa.rs` の subject range 取得は、range 切り出し前の full subject に対して
  exact near-identical 判定を行い、その結果に応じて SEG を適用する順に修正した。
- `LOSAT/tests/run_blastp_outfmt7_comparison.sh` と
  `LOSAT/tests/run_blastp_custom_fields_comparison.sh` を追加し、blastp の
  `outfmt 7` / custom fields acceptance harness を temp output 前提で追加した。
- `cargo build --manifest-path LOSAT/Cargo.toml` は通過した。
- `cd LOSAT/tests && bash run_blastp_comparison.sh` は 2026-03-15 に実行し、
  最初の failing case が `WSSV.PajaWSV.blastp`、その時点の
  `WSSV.faa` vs `PajaWSV.faa` `outfmt 6` は 参照 178 行 / LOSAT 281 行だった。

未完了:

- full `Blast_AdjustScores` parity
- `Blast_RedoOneMatchSmithWaterman`
- unified P
- matrix adjustment rule の full parity
- pairwise formatter

### 10. `composition_adjustment` slice 1 の導線追加
部分完了。

完了した内容:

- `LOSAT/src/core/composition_adjustment/adjust_scores.rs` を追加し、
  `Blast_ReadAaComposition` 相当の amino-acid composition reader、
  `Blast_CompositionPvalue`、`Blast_Overall_P_Value`、
  old-style `Blast_CompositionBasedStats` 相当の scaled-matrix 生成、
  lambda ratio 返却を Rust 側へ追加した。
- `LOSAT/src/core/composition_adjustment/mod.rs` から `adjust_scores` を export した。
- `redo_alignment.rs` の `EMatrixAdjustRule` を NCBI の実値
  `(-1, 0, 1, 2, 3, 4)` に合わせ、`BlastCompo_QueryInfo` の composition を
  `BlastAminoAcidComposition` 保持へ切り替えた。
- `BlastRedoAlignParams` に `matrix_info`、`alphsize`、
  `composition_test_index`、`unified_p`、`log_k` を追加し、
  `BlastRedoOneMatchResult` で `pvalue_for_this_pair` と `lambda_ratio` の返却経路を追加した。
- `blast_redo_one_match` は non-SW branch 内で、subject/query composition を
  window/HSP 単位で読み、zero-composition の nonfatal skip を維持した上で
  `blast_adjust_scores` を呼び、返された `matrix_adjust_rule` と adjusted matrix を
  redo callback へ渡すようにした。
- `kappa.rs` は `read_aa_composition` を使うように変更し、traceback 済み edit script を
  adjusted matrix で再採点して distinct alignment / purge / normalize へ流す経路へ更新した。
- `blast_engine.rs` は `build_matrix_info` を使って `BlastRedoAlignParams` を組み立てるように更新し、
  `smith_waterman: false` と unified P reject は維持した。
- 実測では `WSSV.PajaWSV.blastp` の `outfmt 6` が `281 -> 271` に減少した。

未完了:

- full `Blast_CompositionMatrixAdj` 最適化 branch は未 port であり、
  現在の `adjust_scores.rs` は non-SW slice 1 として old-style scaled matrix へ
  寄せた fallback を使っている。
- `Blast_RedoOneMatchSmithWaterman`、unified P E-value adjustment、
  full matrix-adjust rule parity は未着手のままである。

### 11. protein x-drop raw-score parity と kappa postprocess 順の補正
部分完了。

完了した内容:

- `blast_engine.rs` で protein gapped x-drop を fixed integer ではなく、
  `blast_parameters.c` と同じく bits から raw score に変換して使うように修正した。
  対象は preliminary pass の `gap_x_dropoff` と redo 用 `gap_x_dropoff_final` の両方である。
- `kappa.rs` の `postprocess_preliminary_hits` 入力を拡張し、
  `query_length` と `length_adjustment` を `BlastCompo_QueryInfo` 相当に配線した。
- `kappa.rs` に `blast_kappa.c:135-193` 相当の
  composition-adjusted E-value scaling を追加し、
  `distinct -> HSP化 -> contained reap -> evalue計算 -> composition evalue adjustment ->
  reap by evalue -> normalize -> identities recompute` の順へ修正した。
- `redo_alignment.rs` の `s_WithDistinctEnds` 相当は、
  `num_adjustments == 1` では same-endpoint、
  それ以外では similar-endpoint を使う形に維持しつつ、
  frame 不一致 HSP を distinct-end 比較対象から外すように修正した。
- `blast_engine.rs` に `LOSAT_DIAGNOSTICS=1` 配下の最小診断を追加し、
  先頭 surviving HSP について preliminary boundary と redo boundary を確認できるようにした。

観測された改善:

- `WSSV.faa` vs `PajaWSV.faa` の `outfmt 6` は `271 -> 218` まで低下した。
- 先頭 HSP は参照 `664..1670 / 130..1261 / 598` に対し、
  LOSAT が `659..1660 / 125..1251 / 424` まで近づいた。
- `LOSAT_DIAGNOSTICS=1` では先頭 surviving HSP が
  `prelim q=658..1660 s=124..1251 ... -> redo q=659..1660 s=125..1251 score=1090`
  まで伸びていることを確認した。

未完了:

- 参照 178 行に対して LOSAT 218 行であり、top-hit ordering は維持したが zero diff ではない。
- 先頭差分は純粋な `coordinates` だけではなく、`score/evalue` も大きく残る。
- この slice 後の主残差は redo/kappa 単独よりも、
  `blastp/gapalign.rs` 側の protein gapped scoring / traceback boundary parity に寄っている可能性が高い。

## 2026-03-15 検証

- 実行コマンド: `cargo build --manifest-path LOSAT/Cargo.toml`
  結果: 通過
- 実行コマンド: `cargo test --manifest-path LOSAT/Cargo.toml --lib core::composition_adjustment -- --nocapture`
  結果: 15 件通過
- 実行コマンド: `cargo test --manifest-path LOSAT/Cargo.toml --lib blastp::kappa -- --nocapture`
  結果: 7 件通過
- 実行コマンド: `cd LOSAT/tests && bash run_blastp_comparison.sh`
  結果: 2026-03-15 実行、最初の failing case はまだ `WSSV.PajaWSV.blastp`。
  ただし同ケースの diff は `271 -> 218` へ縮小
- 実行コマンド: `LOSAT/target/debug/LOSAT blastp -query LOSAT/tests/fasta/WSSV.faa -subject LOSAT/tests/fasta/PajaWSV.faa -outfmt 6 -evalue 1e-5 -max_target_seqs 10 -out /tmp/WSSV.PajaWSV.current.out`
  実測: 参照 178 行 / LOSAT 218 行
- 実行コマンド: `LOSAT_DIAGNOSTICS=1 LOSAT/target/debug/LOSAT blastp -query LOSAT/tests/fasta/WSSV.faa -subject LOSAT/tests/fasta/PajaWSV.faa -outfmt 6 -evalue 1e-5 -max_target_seqs 10 -out /tmp/WSSV.PajaWSV.diag.out`
  実測: 先頭 surviving HSP の診断は
  `q_idx=0 s_idx=10 prelim q=658..1660 s=124..1251 gapped=830..344 -> redo q=659..1660 s=125..1251 score=1090`
- `WSSV.PajaWSV.blastp` の先頭差分: line 1、種別は `coordinates + score/evalue`
  - 参照: `YP_009220475.1 ... 664 1670 130 1261 0.0 598`
  - LOSAT: `YP_009220475.1 ... 659 1660 125 1251 1.60e-120 424`
- full fixture sweep: `run_blastp_comparison.sh` が最初の failing case で停止するため未完了
- `LOSAT/tests/run_blastp_outfmt7_comparison.sh` / `LOSAT/tests/run_blastp_custom_fields_comparison.sh`:
  script は存在するが、参照 fixture directory
  `LOSAT/tests/blast_out/blastp_outfmt7` / `LOSAT/tests/blast_out/blastp_custom_fields`
  はまだ未作成のため、このターンでは未実行

## 作業中に新たに判明した課題

### 課題 1. BLASTP の preliminary gapped/restart/redo 接続は進んだが、full parity には届いていない
現行 blastp は、最終 gapped path については `tblastx` 由来の `extend_gapped_protein`
依存を外し、blastp ローカルの traceback-backed 実装へ切り替えた。  
さらに初回 gapped pass は score-only preliminary HSP へ分離され、
restricted/exact の query-local restart、non-SW `Blast_RedoOneMatch`、
protein window 生成、preliminary-HSP ベースの `kappa.rs` 接続も導入された。  
ただし、これはまだ `s_BlastProtGappedAlignment` / `Blast_SemiGappedAlign` /
`s_RestrictedGappedAlign` / `redo_alignment.c` / `blast_kappa.c` の全体を
NCBI と同一にはできていない。

このため、以下の divergence が残る。

- restricted/exact restart は入ったが、trigger hit 位置と tree rebuild 後の再走査順が
  NCBI と bit-perfect かどうかは未確認
- long HSP の boundary / score がまだ NCBI と完全一致していない
- `WSSV.faa` vs `PajaWSV.faa` の `outfmt 6` は 218 行まで下がったが、参照 178 行よりまだ多い
- `adjust_scores.rs` の old-style scaling 導線と x-drop raw-score parity は入ったが、
  先頭差分は `coordinates` に加えて `score/evalue` も残っており、
  次の主残差は `protein gapped scoring / boundary + reap timing + e-value adjustment`
  に寄っている
- `Blast_RedoOneMatchSmithWaterman` と unified P は未 port である

結論:

- blastp core の次工程は、restricted/exact 再始動の細部検証だけではなく、
  `blastp/gapalign.rs` を含む protein gapped scoring / boundary parity と、
  redo/kappa 残差である score adjustment、Smith-Waterman / unified P の復元である。

### 課題 2. `blastp-fast` は task 解決だけで runtime parity ではない
`blastp-fast` の defaults は解決できるようになったが、実行側はまだ word size `5` / compressed lookup / chaining を処理できない。  
現在の runtime validation はここを unsupported として fail fast する。

これは正しい中間状態ではあるが、plan 上は未完了である。

### 課題 3. `--ungapped` と `-use_sw_tback` は surface だけで execution がない
CLI では受けられるが、純 Rust runtime はまだ未 port のため unsupported で停止する。  
これは「勝手に NCBI に委譲しない」という方針には合致しているが、parity scope としては未着手扱いである。

### 課題 4. pairwise formatter がまだ NCBI text を再現していない
`outfmt 0` は hit truth だけではなく formatting block の違いも大きい。

現時点で残っている差分:

- citation block がない
- `Database:` 行の句読点と桁区切りが違う
- subject summary の行構成が違う
- `Method: Compositional matrix adjust.` を含む HSP line まわりの条件が不完全
- alignment row wrapping が違う
- midline の positive 表示が非 parity
- footer の Lambda/K/H と effective search space がない

`outfmt 0` の 8734 行という数値は、hit 数の問題と formatter 欠落が混ざっている。  
したがって、`outfmt 0` の行数 alone では真の改善度を判断できない。

### 課題 5. hitlist / pruning / ordering は blastp 専用化したが timing parity はまだ未完了
`BlastnHitList` / `BlastnHsp` の流用自体は外した。  
しかし、NCBI parity に必要なのは「型名」ではなく「いつ sort / purge / reap / trim するか」であり、そこはまだ完成していない。

現時点で残っている要再点検項目:

- preliminary evalue reap と `Blast_HitListUpdate` の前後関係
- subject summary table と tabular 出力の最終並び検証

補足:

- `Blast_HSPListPurgeHSPsWithCommonEndpoints(..., FALSE)` →
  fragment 再評価 → score 再 sort → containment purge の基本順は blastp path に導入済み。
- `WSSV.faa` vs `PajaWSV.faa` は 218 行まで改善したが、まだ参照 178 行に届かないため、
  現在の主残差は timing だけでなく、後段の exact redo/kappa 実装にも残っている。

### 課題 6. `kappa.rs` は preliminary HSP 入力へ進んだが、full redo/kappa ではない
現状の `kappa.rs` は final `Hit` 直入力ではなく preliminary HSP 入力へ切り替わり、
distinct-end filtering、contained-HSP reap、e-value purge、normalized score、
identity 再計算の順も NCBI 方向へ寄った。  
さらに、non-SW `Blast_RedoOneMatch` と protein window 生成は
`redo_alignment.rs` に移植済みで、arena-backed callback 経由で
preliminary HSP から redone `Hit` を再構成できるようになった。  
ただし、full `blast_kappa.c` / `redo_alignment.c` path ではない。
exact `s_TestNearIdentical` と query-word / SEG gating までは導入したが、
full `Blast_AdjustScores` parity、unified P、`Blast_RedoOneMatchSmithWaterman`、
matrix adjustment rule の full parity、reevaluation timing が未完了である。

これは BLASTP parity の中核課題であり、後段の出力差分の多くはここにも依存する。

### 課題 7. custom tabular fields と outfmt 7 は専用経路へ移したが acceptance 未完了
`outfmt 6` / `outfmt 7` / custom fields は blastp 専用 writer に統一した。  
さらに、`outfmt 7` / custom fields 用の blastp comparison harness は追加した。  
ただし、NCBI の field semantics / header comment / grouping / formatting に対する comparison acceptance はまだ終わっていない。

残課題:

- `outfmt 7` header comment の完全一致確認
- standard field と custom field の整形差分確認
- `qlen` / `slen` / `positive` / `ppos` / `btop` / `stitle` の参照 fixture 生成
- hit truth 安定後の zero-diff sweep

### 課題 8. `tests/unit` の既存 compile breakage が blastp ターゲット test を妨げる
`cargo test --lib blastp::gapalign -- --nocapture` は通るが、
`cargo test blastp::gapalign -- --nocapture` は blastp 新規コードではなく既存
`tests/unit` 側の compile error に止められる。  
今回観測した範囲では、`tests/unit/helpers.rs` の `Hit` 初期化子更新漏れと、
`tests/unit/tblastx/lookup.rs` の `build_ncbi_lookup` 呼び出しシグネチャ不一致がある。

これは blastp parity の本丸ではないが、slice 3 以降の targeted test 実行を妨げる。

## 修正ワークストリームの更新ステータス

### 1. 比較基盤の固定
ステータス: 完了

### 2. Options / task 解決の parity
ステータス: 部分完了

完了:

- task default 解決
- CLI surface の拡張
- `--ungapped` と composition-based stats 禁止

未完了:

- `blastp-fast` runtime
- `--ungapped` runtime
- `-use_sw_tback` runtime

### 3. Lookup / seed 生成の parity
ステータス: 部分完了

完了:

- word size 3 path 上の保存 cutoff 補正
- gapped start seed の window 選択

未完了:

- compressed AA lookup
- word size 5-7 実行経路
- chaining timing

### 4. Ungapped extension の parity
ステータス: 未完了

理由:

- `aa_ungapped.c` ベースの one-hit / two-hit 保存順と再評価タイミングは未整理
- 現在の hit truth は gapped path の影響を受けているため、ungapped 単独評価もまだ不足

### 5. Gapped extension / traceback の parity
ステータス: 作業中

完了:

- gapped-start seed 選択
- interval-tree containment の一部
- `AdjustSubjectRange`
- protein 向け `ALIGN_EX`
- score-only `s_BlastProtGappedAlignment` 相当の初回 gapped pass
- restricted `s_RestrictedGappedAlign` 相当
- `BLAST_GappedAlignmentWithTraceback` 相当の blastp ローカル実装
- traceback ベースの `gap_info` / score / count 再構成
- trimmed HSP の traceback-backed reevaluation helper
- query 単位の `restricted_align_array` と exact restart の導入

未完了:

- restricted/exact restart の trigger / tree rebuild / boundary の完全一致検証
- alignment boundary と score/recount の最終 parity

### 6. Composition-based statistics / kappa parity
ステータス: 作業中

完了:

- preliminary HSP を入力にして traceback 後に postprocess する経路は稼働している
- `redo_alignment.rs` を単一実装源として distinct-end filtering を行う経路は存在する
- non-SW `Blast_RedoOneMatch` と protein window 生成は稼働している
- `adjust_scores.rs` に accurate amino-acid composition reader、
  composition p-value、overall p-value、old-style scaled matrix、lambda ratio 返却を追加した
- `BlastRedoAlignParams` / `BlastRedoOneMatchResult` に
  `matrix_info`、`alphsize`、`composition_test_index`、`unified_p`、`log_k`、
  `pvalue_for_this_pair`、`lambda_ratio` の配線を追加した
- `kappa.rs` は traceback 済み edit script を adjusted matrix で再採点して
  raw score / e-value purge / normalize 順へ流す導線を持つ
- contained-hit reap / e-value purge / normalize / identities recompute の基本順は存在する
- exact `s_TestNearIdentical` と query-word hashing は Rust 側へ移植済み
- full subject に対する exact near-identical 判定後の SEG gating は blastp path に導入済み
- `blast_kappa.c:135-193` 相当の composition-adjusted E-value scaling と
  query context の `query_length` / `length_adjustment` 配線は導入済み

未完了:

- full `Blast_CompositionMatrixAdj` 最適化 branch
- unified P
- `Blast_RedoOneMatchSmithWaterman`
- full `blast_kappa.c`
- matrix adjustment rule の full parity
- reevaluation timing

補足:

- traceback/lifecycle の port 後と score-only 初回 gapped pass / restricted restart 導入後は
  829 行で据え置きだったが、その後の non-SW redo/kappa 接続で 281 行、
  `adjust_scores.rs` 導線追加で 271 行、protein x-drop raw-score parity で 218 行まで下がった。
  ただし参照 178 行との差はまだ残り、先頭 HSP でも `score/evalue` がずれるため、
  現時点では `redo/kappa` 単独ではなく protein gapped scoring も含めた残差が最有力である。

### 7. Hitlist / pruning / ordering parity
ステータス: 作業中

完了:

- extension 前 containment の一部導入
- blastp 専用 `HSPList` / `HitList` 層の追加
- blastp 専用 comparator と `max_hsps_per_subject` / `max_target_seqs` 経路の分離
- blastp path での common-endpoint purge(TRUE) 導入
- blastp path での common-endpoint purge(FALSE) 導入
- trimmed fragment reevaluation
- score 再 sort
- post-traceback containment purge

未完了:

- pruning timing
- preliminary evalue reap の位置
- fixture comparison による ordering 検証

### 8. Output parity
ステータス: 作業中

完了:

- version header 修正
- blastp 専用 tabular writer への統一
- blastp `outfmt 7` comparison harness の追加
- blastp custom fields comparison harness の追加

未完了:

- `outfmt 7` acceptance
- custom field acceptance
- pairwise full text parity

## 今後の方針

### 方針 1. 直近の最優先は protein gapped scoring と redo/kappa 残差の復元である
traceback-backed gapped path、score-only 初回 gapped pass、restricted/exact 再始動、
preliminary-HSP ベースの kappa 接続、non-SW `Blast_RedoOneMatch`、
protein window 生成までは blastp ローカル実装へ切り替わった。  
その結果、`WSSV.faa` vs `PajaWSV.faa` の `outfmt 6` は 218 行まで下がったが、
参照 178 行との残差はまだ大きい。先頭差分も pure coordinates ではなく
`coordinates + score/evalue` に移ったため、次の主対象は
`blastp/gapalign.rs` を含む protein gapped scoring / boundary parity と、
redo/kappa の未 port 領域である `reap timing / e-value adjustment`
である。

これは以下の順で進める。

1. `blastp/gapalign.rs` の protein gapped scoring / traceback boundary を
   `blast_gapalign.c` と照合して詰める
2. non-SW redo 後の `boundary` 残差を `redo_alignment.c` / `blast_kappa.c` に照合して詰める
3. contained-hit reap / distinct-end filtering / e-value purge の timing を `blast_kappa.c` 順へ揃える
4. old-style scaling 導線の上で、必要な `Blast_AdjustScores` 残差を追加で port する
5. `Blast_RedoOneMatchSmithWaterman` と unified P の未対応分岐を port する
6. identities / positives 再計算 / heap insertion timing と restricted/exact restart の boundary を unit test と oracle diff で再検証する
7. 追加済み `outfmt 7` / custom fields harness を参照 fixture 生成後に有効化する

理由:

- traceback/lifecycle だけでなく、score-only 初回 gapped pass / restart までは 829 行で据え置きだったが、
  non-SW redo/kappa 接続、`adjust_scores.rs` 導線追加、protein x-drop raw-score parity で
  218 行まで改善した
- hitlist / tabular path の分離と traceback-backed HSP truth は入ったため、次は
  `postprocess_preliminary_hits` の残差だけでなく、protein gapped scoring / boundary parity を
  `boundary / reap timing / e-value adjustment` と一体で詰め、その後に
  Smith-Waterman / unified P を進める段階である
- `outfmt 6` の hit truth が zero-diff へ近づかない限り、formatter parity を詰めても無駄になる

### 方針 2. `blastp-fast` を surface parity から execution parity へ進める
`blastp-fast` は plan scope の必須項目なので、後回しにはしない。  
ただし順序としては default blastp の hit truth 安定化を先に行う。

具体的には:

1. compressed AA lookup の port
2. word size 5-7 の閾値と lookup selection
3. chaining on の timing
4. `blastp-fast` fixture 追加比較

### 方針 3. pairwise formatter は hit truth 安定後に一気に詰める
`outfmt 0` は scope から外さないが、先に formatter だけ直しても意味が薄い。  
今後は以下の順で詰める。

1. citation block
2. database / query header
3. summary table
4. HSP score/info line
5. alignment rows and midline
6. footer

### 方針 4. 比較は常に current source と temp output で行う
今後も `losat_out/blastp*` は acceptance に使わない。  
比較は毎回 temp output で行い、fixture と current binary のみを比較する。

## 更新後の実装順
1. `blastp/gapalign.rs` の protein gapped scoring / boundary 残差
2. non-SW redo 後の `boundary` 残差
3. contained-hit reap / e-value purge / normalize の timing parity
4. `Blast_AdjustScores` の残差 port
5. `Blast_RedoOneMatchSmithWaterman` / unified P
6. stabilised HSP truth 上で `outfmt 6/7` と custom fields を zero-diff 化
7. `outfmt 0`
8. compressed lookup と `blastp-fast`
9. `--ungapped`
10. `-use_sw_tback`
11. `tests/unit` compile breakage 解消後の targeted test sweep
12. 全 fixture zero-diff sweep

## 完了条件
以下をすべて満たした時点で完了とする。

- BLASTP fixture 全件で `outfmt 6` zero diff
- BLASTP fixture 全件で `outfmt 0` zero diff
- `outfmt 7` と custom field 比較で zero diff
- `blastp` / `blastp-short` / `blastp-fast` の task 別既定値が NCBI と一致
- matrix/gap/word size/composition mode/SEG/ungapped の対応範囲で zero diff
- hit count だけでなく bit score、e-value、座標、順序、header/footer まで一致

## 現時点の結論
2026-03-15 の作業で、BLASTP parity のための足場は前進した。  
比較 harness の偽一致問題は解消し、task/options parity と blastp core のいくつかの大きな divergence は縮小した。  
さらに今回、blastp 専用 `HSPList` / `HitList` / tabular path への分離が入ったことで、`blastn` 型流用と generic writer 依存は blastp engine から外れた。  
加えて、blastp ローカルの traceback-backed gapped path と post-traceback lifecycle も導入し、
score-only 初回 gapped pass、restricted/exact 再始動、preliminary-HSP ベースの
`kappa.rs` 接続に加え、non-SW `Blast_RedoOneMatch` / protein windowing port も導入し、
`cargo build` は通過した。  
ただし、`WSSV.faa` vs `PajaWSV.faa` の最新 `outfmt 6` は 218 行であり、
BLASTP の本質的な未 parity 領域はまだ
`blastp/gapalign.rs / redo_alignment / kappa / pairwise formatting`
に集中している。  
full fixture sweep は未実施であり、現時点では「blastp core の制御フローは NCBI 方向へ進んだが、
主残差はまだ protein gapped scoring と redo/kappa の
`boundary / reap timing / e-value adjustment` に残っている」という段階である。  
今後は exact near-identical 判定、score adjustment、Smith-Waterman / unified P、
restricted/exact の細部も含めて hit truth を安定化させた後に、
`outfmt 6/7/custom/0` を zero-diff まで追い込む。

## 2026-03-15 追加実装ログ: BLASTP core parity slice

この追記は、default `blastp` core の parity に限定した追加実装と、その時点での観測結果を記録する。

### 今回削除した非 NCBI 実装

- `kappa.rs` の redo 後再スコアを削除した。
  - NCBI `blast_kappa.c:1902-1945` / `1748-1770` は、`BLAST_GappedAlignmentWithTraceback` の score を
    `s_NewAlignmentFromGapAlign` 経由でそのまま distinct alignment に渡しており、LOSAT 側の追加 rescoring は
    NCBI に存在しない。
- `s_HSPListNormalizeScores` 相当の bit score 計算で、未縮小の `Lambda` を使っていた誤りを除去した。
  - NCBI `blast_kappa.c:102-116` / `2117-2133` では、rescaled search 後の `kbp->Lambda` を使う。

### 今回 NCBI に合わせて修正した箇所

- `adjust_scores.rs`
  - `build_matrix_info` の `start_matrix` を、`blast_kappa.c:2216-2231` に合わせて
    `start_freq_ratios` と `ungappedLambda / scale_factor` から再構築した。
- `blast_engine.rs`
  - `redo` 用 `score_divisor` を `SCALING_FACTOR=32` に合わせた。
  - `BlastCompo_GappingParams` の `gap_open/gap_extend` は `s_RescaleSearch`
    (`blast_kappa.c:2117-2133`) と同じく scale 後の値を持つようにした。
  - `redo` 用 `x_dropoff` は、scale 済み `Lambda` から取り直す必要があるため、
    単純な raw-score 倍算ではなく NCBI の bits-to-raw 計算へ寄せた。
- `gapalign.rs`
  - redo traceback path で composition-adjusted matrix を実際に DP に通すようにした。
  - `ALIGN_EX` / `Blast_SemiGappedAlign` の forward 側は、current call-site の
    slice 形状が `query+q_start` / `subject+s_start` に対応していることを再確認した。
    この slice では forward indexing 自体の追加変更は入れていない。
- `kappa.rs`
  - `evaluate_and_purge` は scaled `Lambda` で E-value / bit score を計算し、
    `normalize_scores` も rescaled search 後の `Lambda` を使うようにした。

### 今回の測定結果

- targeted verification
  - `cargo test --manifest-path LOSAT/Cargo.toml --lib test_reevaluate_blastp_hit_`: 通過
  - `cargo test --manifest-path LOSAT/Cargo.toml --lib test_windows_from_protein_aligns_`: 通過
  - `cargo build --manifest-path LOSAT/Cargo.toml`: 通過
- `WSSV.faa` vs `PajaWSV.faa` (`outfmt 6`)
  - 参照: `178`
  - この slice 開始時 baseline: `224`
  - 最新 current binary: `217`
  - `redo` 入力 score の truncation と `cutoff_s` の `(int)` cast を NCBI に合わせたことで、
    fixture 差分は `224 -> 217` に縮小した

### 未実装

- `blast_gapalign.c:4549-4712` 相当の `BLAST_GappedAlignmentWithTraceback` 本体は、left/right traceback の edit-script merge と seed 境界をまだ全文照合し切っていない。
- `blast_kappa.c` 側の `preliminary -> redo -> reap/evalue purge -> normalize -> identity recompute` の時系列と、query 単位の restart 境界はまだ full parity ではない。
- `Blast_AdjustScores` の full port、Smith-Waterman redo、unified P は未 port のままである。
- `blastp-fast` は task surface まではあるが runtime parity ではない。
- `outfmt 7` fixture / custom fields fixture / pairwise formatter の全文 parity は未着手の残差がある。

### 原因仮説

- 今回直したのは post-traceback reevaluation の途中再始動と、redo window の固定 slot compact/sort であり、これで `gap_info` の左端切り詰め誤差は 1 つ除去できた。
- 追加診断で、anchor case の `Blast_AdjustScores` は `matrix_rule=CompoScaleOldMatrix` として実際に発火していた。
  したがって主残差は「adjust が走っていない」ことではなく、adjust 後の `redo gapalign`
  か SEG 済み subject 上の境界選択に残っている可能性が高い。
- `WSSV.PajaWSV.blastp` の主残差は依然として `redo gapalign` の左境界、`Blast_PrelimEditBlockToGapEditScript` 相当の merge、redo 後 purge/evalue timing のいずれかに残っている。
- `ALIGN_EX` forward indexing については今回 call-site の slice 形状まで再確認し、現行の `B[b_index+1]` 対応は `query+q_start` / `subject+s_start` と整合するため、ここは今回の差分原因ではないと判断した。

### 検証結果

- `gapalign.rs`
  - `Blast_HSPReevaluateWithAmbiguitiesGapped` の途中 reset 時に、substitution run の途中から再開する場合の `esp->num[index] -= op_index` を Rust 側にも入れた。
  - `s_UpdateReevaluatedHSP` 相当の末尾 `best_end_esp_num` 反映まで含め、`gap_info` を最良部分列へ切り詰めるように置換した。
  - 新規 unit test `test_reevaluate_blastp_hit_trims_partial_leading_sub_run` を追加し、先頭 mismatch を跨いだ `Sub(4) -> Sub(3)` trim を確認した。
- `redo_alignment.rs`
  - `s_WindowsFromProteinAligns` に合わせ、window 配列を query index の固定 slot で作り、後段で compact する流れに置換した。
  - 動的 resize と `max(0)` 補正に依存する経路を除去した。
- `kappa.rs`
  - `s_HSPListFromAlignments` 相当の incoming alignment 作成で、
    `BlastCompo_AlignmentNew((int)(hsp->score * localScalingFactor), hsp->context, ...)`
    の `(int)` cast を `round` ではなく truncation に戻した。
  - `queryIndex` も local zero 固定ではなく HSP 側 context 相当に戻した。
- `blast_engine.rs`
  - `s_GetAlignParams` の `cutoff_s = (int)(hitParams->cutoff_score_min * localScalingFactor)`
    を `round` ではなく truncation に戻した。
- 測定コマンド
  - `cargo test --manifest-path LOSAT/Cargo.toml --lib test_reevaluate_blastp_hit_`: 2 件通過
  - `cargo test --manifest-path LOSAT/Cargo.toml --lib test_windows_from_protein_aligns_`: 2 件通過
  - `cargo build --manifest-path LOSAT/Cargo.toml`: 通過
  - `cargo test --manifest-path LOSAT/Cargo.toml --lib test_build_incoming_alignment_list_uses_truncating_scaled_score_and_query_index -- --nocapture`: 通過
  - `cargo test --manifest-path LOSAT/Cargo.toml --lib test_blastp_redo_params_follow_ncbi_cutoff_formulas -- --nocapture`: 通過
  - `LOSAT/target/debug/LOSAT blastp -query LOSAT/tests/fasta/WSSV.faa -subject LOSAT/tests/fasta/PajaWSV.faa -outfmt 6 -evalue 1e-5 -max_target_seqs 10 -out /tmp/WSSV.PajaWSV.current.out`: `217/178`
  - 一時診断: `LOSAT_DIAGNOSTICS=1 ... | rg '\\[BLASTP REDO\\]|\\[BLASTP KAPPA\\]'`
    - anchor `q_idx=0 s_idx=10` で `matrix_rule=CompoScaleOldMatrix`, `adjusted_matrix=true`,
      `prelim q=658..1659 s=124..1250 gapped=830..344 -> redo q=610..1659 s=124..1250 score=1204`

### 次の検証ゲート

1. `gapalign`
   - `WSSV.faa` vs `PajaWSV.faa` の current binary 差分 `218/178` を起点に、先頭 HSP の `qstart/qend/sstart/send/bit score` が `blast_gapalign.c:4549-4712` と `blast_gapalign.c:2481-2534` のどこでずれるかを再局所化する。
2. `redo/kappa timing`
   - `blast_kappa.c` の `redo -> reap/evalue purge -> normalize -> identity recompute` を再照合し、restart 境界と purge 順を query 単位で揃える。
3. `blastp-fast`
   - compressed lookup / chaining を runtime で実装するか、未対応条件を明示的 unsupported に統一する。
4. `formatter`
   - `outfmt 6/7` の hit truth が zero-diff へ近づいてから `outfmt 0` 本文・summary・footer を全文再現する。

### 作業木メモ

- `blast_out` に存在する BLASTP fixture は引き続き `blastp` と `blastp_pairwise` のみである。
- `LOSAT/tests/unit/helpers.rs` と `LOSAT/tests/unit/tblastx/lookup.rs` の drift はまだ残っている。
- 作業木は引き続き広く dirty であり、今回の slice では BLASTP core parity に直接関係する差分のみを触った。

## 2026-03-16 追補: gapalign merge/prune と internal-coordinate reap

この追補は、`2026-03-16 00:18 JST` 時点で追加実施した default `blastp`
parity 修正と、その結果を記録する。

### 今回 NCBI に合わせて修正した箇所

- `LOSAT/src/algorithm/blastp/gapalign.rs`
  - `Blast_PrelimEditBlockToGapEditScript`
    (`blast_gapalign.c:2481-2534`) の merge 規則を全文照合し、
    `fwd_prelim_tback` の末尾 op を使う merge、forward 側の逆順連結、
    merge 済み先頭 op の skip を NCBI 通りに置換した。
  - `BLAST_GappedAlignmentWithTraceback`
    (`blast_gapalign.c:4549-4712`) の right extension 条件を
    `q_start < q_length && s_start < s_length` に戻した。
  - leading/trailing gap prune は `blast_gapalign.c:4681-4707` に合わせたまま、
    score と boundary の同時更新を unit test で固定した。
- `LOSAT/src/algorithm/blastp/kappa.rs`
  - `s_HitlistReapContained`
    (`blast_kappa.c:224-260`) に合わせ、contained purge を user-facing の
    1-based/inclusive 座標ではなく、redo 直後の internal offsets
    (`query_start/query_end/subject_start/subject_end`) で行うように置換した。
- `LOSAT/src/core/composition_adjustment/redo_alignment.rs`
  - `Blast_RedoOneMatch`
    (`redo_alignment.c:1277-1292`) に NCBI には存在しない final
    `distinct_alignments_sort` を削除した。

### 追加した unit test

- `LOSAT/src/algorithm/blastp/gapalign.rs`
  - `test_prelim_edit_ops_to_gap_edit_script_reverses_forward_ops_ncbi_style`
  - `test_prelim_edit_ops_to_gap_edit_script_merges_last_forward_op_ncbi_style`
  - `test_prune_terminal_gap_ops_updates_score_and_boundaries_ncbi_style`
- `LOSAT/src/algorithm/blastp/kappa.rs`
  - `test_reap_contained_hits_uses_internal_offsets_ncbi_style`

### 実施コマンド

- `cargo test --manifest-path LOSAT/Cargo.toml --lib algorithm::blastp::gapalign::tests:: -- --nocapture`
- `cargo test --manifest-path LOSAT/Cargo.toml --lib algorithm::blastp::kappa::tests:: -- --nocapture`
- `cargo test --manifest-path LOSAT/Cargo.toml --lib core::composition_adjustment::redo_alignment::tests:: -- --nocapture`
- `cargo build --manifest-path LOSAT/Cargo.toml`
- `LOSAT/target/debug/LOSAT blastp -query LOSAT/tests/fasta/WSSV.faa -subject LOSAT/tests/fasta/PajaWSV.faa -outfmt 6 -evalue 1e-5 -max_target_seqs 10 -out /tmp/WSSV.PajaWSV.current.out`
- `diff -u LOSAT/tests/blast_out/blastp/WSSV.PajaWSV.blastp.out /tmp/WSSV.PajaWSV.current.out | sed -n '1,80p'`
- `cd LOSAT/tests && bash run_blastp_comparison.sh`

### 観測値

- targeted unit test
  - `gapalign::tests`: 9 件通過
  - `kappa::tests`: 10 件通過
  - `redo_alignment::tests`: 11 件通過
- `cargo build --manifest-path LOSAT/Cargo.toml`: 通過
- `WSSV.faa` vs `PajaWSV.faa` (`outfmt 6`)
  - 参照: `178`
  - 今回作業開始時の直前 baseline: `217`
  - 最新 current binary: `218`
  - 先頭 HSP: ref `664..1670 / 130..1261 / 598` に対し current
    `610..1659 / 124..1250 / 468`
- `cd LOSAT/tests && bash run_blastp_comparison.sh`
  - 最初の failing case は引き続き `WSSV.PajaWSV.blastp`

### 結果

- 改善なし。
- contained reap の internal-coordinate 化と `Blast_RedoOneMatch` 末尾の sort 削除は、
  `WSSV.PajaWSV.blastp` の line count を `218/178` から動かさなかった。
- `Blast_PrelimEditBlockToGapEditScript` / traceback right-boundary 修正後も、
  anchor HSP の `redo q=610..1659 s=124..1250` は変わらず、主残差はまだ
  redo traceback 本体に残っている。

### 事実ベースの残差候補

- `blast_gapalign.c:4549-4712`
  - `BLAST_GappedAlignmentWithTraceback` / `ALIGN_EX` の traceback path 自体に、
    まだ NCBI と異なる tie-break / boundary 取り扱いが残っている可能性がある。
- `composition_adjustment`
  - `comp_based_stats=2` の adjusted matrix 生成は、現状 `adjust_scores.rs` の
    old-style scaled matrix 寄り実装に依存しており、
    `Blast_CompositionMatrixAdj` を含む full NCBI flow との差が残っている可能性がある。
- `redo gapalign`
  - anchor case の preliminary HSP 自体は `q=658..1659 s=124..1250 gapped=830..344`
    であり、最終 HSP の大きな左伸長は redo traceback 段で発生している。

## 2026-03-16 追補: kappa/hsp-list/evalue lifecycle 置換

この追補は、`2026-03-16 11:55 JST` 時点で追加実施した `blastp`
postprocess parity 修正と、その結果を記録する。

### 今回 NCBI に合わせて修正した箇所

- `LOSAT/src/core/composition_adjustment/adjust_scores.rs`
  - `Blast_AdjustScores`
    (`composition_adjustment.c:1414-1530`) の戻り契約を NCBI に合わせた。
  - query/subject の true amino acid 数が 0 の場合は、adjusted matrix を捏造せず
    nonfatal status 相当で caller に戻すようにした。
  - matrix-adjust 成功時は `ratioToPassBack = 1.0` 相当だけを返し、
    Rust 独自の `used_nonfatal_fallback` 状態は削除した。
- `LOSAT/src/core/composition_adjustment/redo_alignment.rs`
  - `Blast_RedoOneMatch`
    (`redo_alignment.c:1219-1269`) の `Blast_AdjustScores` 呼び出し後処理を
    NCBI 順に戻し、nonfatal status では redo alignment を実行しない形に置換した。
- `LOSAT/src/algorithm/blastp/hsp.rs`
  - `s_BlastGetBestEvalue`
    (`blast_hits.c:1739-1748`) と `Blast_HSPListReapByEvalue`
    (`blast_hits.c:1976-2004`) 相当 helper を blastp HSP list 側へ追加した。
- `LOSAT/src/algorithm/blastp/kappa.rs`
  - `s_HSPListFromDistinctAlignments`
    (`blast_kappa.c:305-354`) 相当として、distinct alignment arena から
    `BlastpHspList` を構築する経路を追加した。
  - `s_HitlistReapContained`
    (`blast_kappa.c:224-260`) は internal offsets を使って HSP list と
    redo arena を同時に削る形へ置換した。
  - `s_AdjustEvaluesForComposition`
    (`blast_kappa.c:135-193`) と `s_HitlistEvaluateAndPurge`
    (`blast_kappa.c:395-457`) は `BlastpHspList` を直接更新する形へ置換し、
    `Blast_HSPListGetEvalues -> composition evalue adjust -> Blast_HSPListReapByEvalue`
    の順を固定した。
  - `postprocess_preliminary_hits`
    (`blast_kappa.c:3658-3710`) は `Vec<RedoneBlastpHit>` 直処理から外し、
    `HSP list -> evaluate/purge -> normalize -> identities` の順へ戻した。

### 追加・更新した unit test

- `LOSAT/src/core/composition_adjustment/adjust_scores.rs`
  - `test_blast_adjust_scores_returns_none_for_zero_composition_ncbi_style`
- `LOSAT/src/algorithm/blastp/kappa.rs`
  - `test_reap_contained_hits_uses_internal_offsets_ncbi_style`
  - `test_adjust_evalues_for_composition_matches_ncbi_sequence_scaling`
  - `test_reap_contained_hits_discards_lower_scoring_inner_hit`

### 実施コマンド

- `cargo build --manifest-path LOSAT/Cargo.toml`
- `cargo test --manifest-path LOSAT/Cargo.toml --lib algorithm::blastp::kappa::tests:: -- --nocapture`
- `cargo test --manifest-path LOSAT/Cargo.toml --lib algorithm::blastp::hsp::tests:: -- --nocapture`
- `cargo test --manifest-path LOSAT/Cargo.toml --lib core::composition_adjustment::adjust_scores::tests:: -- --nocapture`
- `LOSAT/target/debug/LOSAT blastp -query LOSAT/tests/fasta/WSSV.faa -subject LOSAT/tests/fasta/PajaWSV.faa -outfmt 6 -evalue 1e-5 -max_target_seqs 10 -out /tmp/WSSV.PajaWSV.current.out`
- `wc -l LOSAT/tests/blast_out/blastp/WSSV.PajaWSV.blastp.out /tmp/WSSV.PajaWSV.current.out`
- `diff -u LOSAT/tests/blast_out/blastp/WSSV.PajaWSV.blastp.out /tmp/WSSV.PajaWSV.current.out`

### 観測値

- `cargo build --manifest-path LOSAT/Cargo.toml`: 通過
- targeted unit test
  - `algorithm::blastp::kappa::tests::`: 11 件通過
  - `algorithm::blastp::hsp::tests::`: 3 件通過
  - `core::composition_adjustment::adjust_scores::tests::`: 6 件通過
- `WSSV.faa` vs `PajaWSV.faa` (`outfmt 6`)
  - 参照: `178`
  - 今回作業開始時 baseline: `218`
  - 最新 current binary: `174`
- 先頭 diff 行
  - 参照: `YP_009220475.1 ... 664 1670 130 1261 0.0 598`
  - current: `YP_009220475.1 ... 664 1670 130 1261 6.79e-173 599`
- 先頭 HSP では coordinates / `%identity` / `alignment length` / `mismatch` / `gapopen`
  は一致した。
- 残差は主に `evalue / bit score` のずれと、全体 `174/178` の hit-count 差へ移った。

### 結果

- 改善あり。
- `blastp` 後段を HSP list lifecycle に戻したことで、`WSSV.PajaWSV.blastp` は
  `218/178 -> 174/178` まで改善した。
- ただし zero diff ではないため、`run_blastp_comparison.sh` の full sweep にはまだ広げていない。

### 次の残差候補

- `blast_kappa.c:102-116`
  - `s_HSPListNormalizeScores` 後の raw score 丸めと bit score 計算がまだ 1 raw-score
    ずれるケースが残っている。
- `blast_kappa.c:135-193`
  - composition-adjusted evalue の scale 後に、NCBI の `0.0` clipping と一致しない
    case が残っている。
- `blast_kappa.c:395-457` / `blast_hits.c:1976-2004`
  - `Blast_HSPListReapByEvalue` 後に消えている 4 hit は、まだ purge timing か
    final hitlist 反映順に差がある可能性が高い。

## 2026-03-16 追補: blast_hits / kappa normalize-prune-hitlist order

この追補は、`2026-03-16 12:20 JST` 時点で追加実施した `blastp`
core parity slice の実装結果を記録する。

### 今回 NCBI に合わせて置換した関数

- `LOSAT/src/algorithm/blastp/hsp.rs`
  - `Blast_HSPListReapByEvalue`
    (`blast_hits.c:1976-1999`) を `retain` ベースの簡略実装から、
    NCBI と同じ order-preserving compaction へ置換した。
  - `Blast_TrimHSPListByMaxHsps`
    (`blast_hits.c:2049-2067`) の truncate timing を明示し、
    `blast_traceback.c:845-852` に合わせて final hitlist sort 後にだけ
    適用するようにした。
  - `Blast_HitListUpdate`
    (`blast_hits.c:3243-3297`) の tie case を unit test で固定し、
    equal `evalue/score` では newer subject が older worst hit を
    押し出し得る NCBI 挙動を確認した。
- `LOSAT/src/algorithm/blastp/kappa.rs`
  - `s_HSPListNormalizeScores`
    (`blast_kappa.c:102-116`) を `Hit` 直配列ではなく `BlastpHspList`
    へ直接適用する形へ戻し、`BLAST_Nint` 相当の丸めを helper 化した。
  - `s_AdjustEvaluesForComposition`
    (`blast_kappa.c:135-193`) は、HSP list が空でも
    `best_evalue = DBL_MAX` 相当を保持する形へ戻した。
  - `postprocess_preliminary_hits`
    (`blast_kappa.c:3658-3710`) は
    `evaluate/purge -> normalize -> identities` の順を維持し、
    normalize を final `Hit` 再構築の前に戻した。
- `LOSAT/src/algorithm/blastp/blast_engine.rs`
  - `Blast_TrimHSPListByMaxHsps` 相当の呼び出し位置を、
    subject HSP list 構築直後ではなく
    `Blast_HitListSortByEvalue` 後の final hitlist phase
    (`blast_traceback.c:845-852`) に移した。

### 追加・更新した unit test

- `LOSAT/src/algorithm/blastp/kappa.rs`
  - `test_normalize_scores_uses_blast_nint_and_scaled_bit_score`
- `LOSAT/src/algorithm/blastp/hsp.rs`
  - `test_reap_hsplist_by_evalue_preserves_ncbi_compaction_order`
  - `test_hit_list_update_replaces_equal_worst_with_newer_oid`

### 実施コマンド

- `cargo build --manifest-path LOSAT/Cargo.toml`
- `cargo test --manifest-path LOSAT/Cargo.toml --lib blastp::kappa -- --nocapture`
- `cargo test --manifest-path LOSAT/Cargo.toml --lib blastp::hsp -- --nocapture`
- `cd LOSAT/tests && bash run_blastp_comparison.sh`
- `LOSAT/target/debug/LOSAT blastp -query LOSAT/tests/fasta/WSSV.faa -subject LOSAT/tests/fasta/PajaWSV.faa -outfmt 6 -evalue 1e-5 -max_target_seqs 10 -out /tmp/WSSV.PajaWSV.current.out`
- `cd LOSAT/tests && wc -l ./blast_out/blastp/WSSV.PajaWSV.blastp.out /tmp/WSSV.PajaWSV.current.out`

### 結果

- `cargo build --manifest-path LOSAT/Cargo.toml`: 通過
- `cargo test --manifest-path LOSAT/Cargo.toml --lib blastp::kappa -- --nocapture`: 12 件通過
- `cargo test --manifest-path LOSAT/Cargo.toml --lib blastp::hsp -- --nocapture`: 5 件通過
- `cd LOSAT/tests && bash run_blastp_comparison.sh`
  - 依然として最初の failing case は `WSSV.PajaWSV.blastp`
  - `WSSV.PajaWSV.blastp` を越えて full sweep には進めていない
- `WSSV.faa` vs `PajaWSV.faa` (`outfmt 6`)
  - 参照: `178`
  - 最新 current binary: `174`
- 先頭 diff 行
  - 参照: `YP_009220475.1 ... 664 1670 130 1261 0.0 598`
  - current: `YP_009220475.1 ... 664 1670 130 1261 6.79e-173 599`

### 切り分け

- 今回の slice 後も、先頭 HSP の coordinates / `%identity` / `alignment length` /
  `mismatch` / `gapopen` は一致している。
- したがって、主 blocker は formatter ではない。
- また、main residual は traceback boundary そのものでもなく、
  `s_HSPListNormalizeScores` / `s_AdjustEvaluesForComposition` /
  `Blast_HSPListReapByEvalue` / `Blast_HitListUpdate` 周辺の
  `evalue / bit score / prune timing` に残っている。

### 次の着手点

1. `blast_kappa.c:102-116`
   - `s_HSPListNormalizeScores` の raw score 丸めと bit score 計算で、
     `599 -> 598` の 1 raw-score 差がどこで残るかを固定する。
2. `blast_kappa.c:135-193`
   - composition-adjusted evalue を `0.0` clipping まで含めて追い込み、
     `6.79e-173 -> 0.0` を消す。
3. `blast_kappa.c:395-457` / `blast_hits.c:1976-2004` / `blast_hits.c:3243-3297`
   - `174/178` の hit-count 差を、reap timing と final hitlist replacement 順の
     照合で潰す。

## 2026-03-19 追記: AP027131/NZ_CP006932 upstream endpoint parity 再照合

### 除去した NCBI 差分

- `LOSAT/src/algorithm/blastp/gapalign.rs`
  - NCBI `blast_gapalign.c:4247-4318` に合わせて、
    `s_BlastProtGappedAlignment` の protein score-only preliminary path で
    `q_length/s_length` と `AdjustSubjectRange` 後の no-right endpoint
    (`query_stop = q_start - 1`, `subject_stop = s_start - 1`) をそのまま使う形へ置換した。
  - preliminary HSP の座標型を `usize` 由来の saturating semantics から、
    NCBI `BlastHSP` と同じ signed endpoint semantics に寄せた。
- `LOSAT/src/algorithm/blastp/kappa.rs`
  - NCBI `blast_kappa.c:1757-1770` に合わせて、
    redo traceback 後の `query_stop <= query_start` /
    `subject_stop <= subject_start` を理由に alignment を捨てる
    LOSAT 独自 rejection を削除した。
- `LOSAT/src/algorithm/blastp/gapalign.rs`
  - NCBI `blast_hits.c:151-182` に合わせて、
    preliminary traceback helper 側の同種 endpoint rejection も削除した。

### 変更した LOSAT ファイル

- `LOSAT/src/algorithm/blastp/gapalign.rs`
- `LOSAT/src/algorithm/blastp/blast_engine.rs`
- `LOSAT/src/algorithm/blastp/kappa.rs`

### 実行した最終確認コマンド

- `cargo build --manifest-path /mnt/c/Users/genom/GitHub/LOSAT/LOSAT/Cargo.toml --release --bin LOSAT`
- `cd /mnt/c/Users/genom/GitHub/LOSAT/LOSAT && ./target/release/LOSAT blastp -query ./tests/fasta/AP027131.faa -subject ./tests/fasta/NZ_CP006932.faa -outfmt 6 -evalue 1e-5 -max_target_seqs 10 -out /tmp/AP027131.NZ_CP006932.after_kappa_guard.out`
- `diff -u /mnt/c/Users/genom/GitHub/LOSAT/LOSAT/tests/blast_out/blastp/AP027131.NZ_CP006932.blastp.out /tmp/AP027131.NZ_CP006932.after_kappa_guard.out`

### 結果

- `cargo build --release --bin LOSAT`: 通過
- target diff は zero diff にならず、残差は 5 本のまま不変
- 現在の残差:
  - missing: `BDV02221.1 / WP_025208963.1`
  - missing: `BDV02412.1 / WP_025208950.1`
  - short-vs-long: `BDV02435.1 / WP_025208655.1`
  - missing: `BDV02435.1 / WP_025208654.1`
  - missing: `BDV02649.1 / WP_025208424.1`

### 残 blocker と次の upstream 優先順位

- no-right endpoint 自体の port は入ったが、NCBI と同じ endpoint を downstream がまだ同じように消費できていない。
- 次の優先順位:
  1. `blast_kappa.c:770-803` / `1902-1945` / `redo_alignment.c` の incoming distinct-alignment 生成順と endpoint distinctness を再照合する
  2. それで足りなければ `blast_gapalign.c:3870-4010` から `Blast_HSPListPurgeHSPsWithCommonEndpoints` までの preliminary HSP admission / purge / sort 順を再照合する

## 2026-03-19 再追記: blastp upstream HSP admission parity slice

### この追記の位置づけ

- この節が 2026-03-19 時点の latest / authoritative state である。
- 同日 earlier note に残っている `2 HSP` 観測は current state ではない。最終比較をやり直した結果、current residual はこの節の `5 本` が正しい。

### この slice で除去した NCBI 差分

#### 1. preliminary HSP が `context/frame` を暗黙同値に依存していた差分
完了。

実施内容:

- `LOSAT/src/algorithm/blastp/gapalign.rs` の `BlastpPreliminaryHsp` に、NCBI `Blast_HSPInit(..., context, query_frame, subject->frame, ...)` に対応する `query_context` と `subject_frame` を追加した。
- `LOSAT/src/algorithm/blastp/blast_engine.rs` で、NCBI `blast_hits.c:2268-2379` に合わせて preliminary common-endpoint comparator が `q_idx` ではなく `context` を比較するよう修正した。
- `LOSAT/src/algorithm/blastp/blast_engine.rs` で、NCBI `blast_gapalign.c:3975-4000` に合わせて interval-tree rebuild が `q_idx == context` の暗黙同値ではなく、preliminary HSP 自身の `query_context` を唯一の truth として使うよう修正した。
- `LOSAT/src/algorithm/blastp/kappa.rs` で、NCBI `blast_kappa.c:770-803` に合わせて incoming alignment が `queryIndex = hsp->context`、`frame = hsp->subject.frame` を使うよう修正した。

意味:

- これは blastp でたまたま成立していた `q_idx == context` / `query_frame == subject.frame` の暗黙依存を除去し、NCBI source の責務分割そのものへ戻した修正である。
- distinctness や downstream suppression をいじったのではなく、distinctness に入る前の入力を source に合わせた。

#### 2. upstream path に残っていた saturating / clamp / silent drop
完了。

実施内容:

- `LOSAT/src/algorithm/tblastx/blast_gapalign.rs` の shared `BLAST_GetUngappedHSPList` helper で、NCBI `blast_gapalign.c:2384-2392` と `4756-4768` に合わせて signed context-relative offsets を先に検証し、`usize` への wrap と `saturating_sub` を削除した。
- `LOSAT/src/algorithm/blastp/blast_engine.rs` で、NCBI `blast_gapalign.c:3937-3945` に合わせて `gapped_subject_start = s_off + (max_offset - q_off)` を signed `Int4` と同じ形で計算し、`checked_sub` / `saturating_add` による silent drop を削除した。
- `LOSAT/src/algorithm/blastp/gapalign.rs` で、NCBI `blast_gapalign.c:4163-4318` に合わせて `AdjustSubjectRange`、`q_length - private_q_start`、`s_length - private_s_start + subject_shift`、および no-right endpoint の `q_start - 1` / `s_start - 1` を direct arithmetic に置換した。
- `LOSAT/src/algorithm/blastp/blast_engine.rs` で、preliminary HSP から provisional hitlist へ落とす変換の `max(0)` / `unwrap_or(0)` clamp を削除し、NCBI 前提の ordered signed endpoints をそのまま要求する fail-fast 形へ置換した。

意味:

- これはズレた結果を後段で丸める修正ではない。silent correction を削除し、NCBI と同じ入力を downstream へ渡すための置換である。

### この slice で実行した確認

実行コマンド:

```bash
cargo test --manifest-path LOSAT/Cargo.toml --lib blastp::blast_engine -- --nocapture
cargo test --manifest-path LOSAT/Cargo.toml --lib blastp::kappa -- --nocapture
cargo build --manifest-path LOSAT/Cargo.toml --release --bin LOSAT
cd LOSAT && ./target/release/LOSAT blastp -query tests/fasta/AP027131.faa -subject tests/fasta/NZ_CP006932.faa -outfmt 6 -evalue 1e-5 -out /tmp/AP027131.NZ_CP006932.release.out
cd LOSAT && diff -u tests/blast_out/blastp/AP027131.NZ_CP006932.blastp.out /tmp/AP027131.NZ_CP006932.release.out
```

結果:

- `cargo test --manifest-path LOSAT/Cargo.toml --lib blastp::blast_engine -- --nocapture`: 18 件通過
- `cargo test --manifest-path LOSAT/Cargo.toml --lib blastp::kappa -- --nocapture`: 15 件通過
- `cargo build --manifest-path LOSAT/Cargo.toml --release --bin LOSAT`: 通過
- `AP027131.faa` vs `NZ_CP006932.faa`: zero diff にはならず、residual は `5 本`
- この residual list は本書 earlier same-day note の `2 HSP` ではなく、同日後段の `5 本` 観測と一致した
- gating diff が nonzero のため、`cd LOSAT/tests && bash run_blastp_comparison.sh` は未実行

### current residual

- missing: `BDV02221.1 / WP_025208963.1`
- missing: `BDV02412.1 / WP_025208950.1`
- short-vs-long: `BDV02435.1 / WP_025208655.1`
- missing: `BDV02435.1 / WP_025208654.1`
- missing: `BDV02649.1 / WP_025208424.1`

### 現時点の判断

- `preliminary HSP -> kappa incoming alignment` の `context/frame` drift と、shared helper / score-only preliminary path の saturating semantics は 2026-03-19 時点で除去済みである。
- それでも residual が `5 本` のまま不変である以上、主犯はまだ upstream admission 本体か、その直後の preliminary purge / sort 順に残っている。
- 次の優先順位は以下に更新する。

1. `blast_gapalign.c:3870-4010` の preliminary HSP admission 順、`Blast_HSPListSaveHSP`、tree update timing を `InitHSP -> UngappedHit -> preliminary HSP` の全入力順込みで再照合する。
2. それでも不変なら `aa_ungapped.c:804-1177` と `blast_extend.c:269-375` の hit 生成順へ戻り、`BDV02435.1` の longer HSP と missing 4 本がどの stage で脱落するかを source comparison だけで特定する。
3. formatter 側、post-filter 側、oracle 依存の suppression は引き続き禁止する。

## 2026-03-19 再々追記: blastp upstream admission fail-fast / subject-frame equality slice

### この追記の位置づけ

- この節が 2026-03-19 時点の latest / authoritative state である。
- 同日 earlier note に残っている「protein score-only preliminary path の no-right endpoint は `q_start - 1` / `s_start - 1`」という記述は NCBI source と不一致であり、この節の内容で superseded とする。

### この slice で除去した NCBI 差分

#### 1. shared `BLAST_GetUngappedHSPList` helper の silent drop
完了。

実施内容:

- `LOSAT/src/algorithm/tblastx/blast_gapalign.rs` で、NCBI `blast_gapalign.c:2384-2392` と `4756-4768` に合わせて、context-relative `q_start/q_end/q_off` と subject-side `s_start/s_end/s_off` の不整合を `continue` で捨てる経路を削除した。
- 同 helper は `Blast_HSPInit(...)` へ渡す signed offsets を前提条件として要求し、Rust 側独自の silent drop ではなく fail-fast に置換した。
- unit test では、negative context-relative query start を `should_panic` で固定した。

意味:

- これは NCBI `BLAST_GetUngappedHSPList` の「有効な `InitHSP` をそのまま `Blast_HSPInit` へ流す」責務への復元であり、後段補正ではない。

#### 2. preliminary purge / retry / query grouping の `q_idx` shortcut
完了。

実施内容:

- `LOSAT/src/algorithm/blastp/blast_engine.rs` で、NCBI `blast_gapalign.c:3753-3765` と `3870-4000` に合わせて、`build_restricted_align_array`、`remove_preliminary_hits_for_query`、`split_preliminary_hits_by_query` が `q_idx` ではなく `context -> query_index` を唯一の truth とするよう修正した。
- preliminary common-endpoint purge では、NCBI `blast_hits.c:2482-2487` / `2508-2513` に合わせて `subject.frame` の完全一致条件を復元した。
- unit test では、`q_idx` を故意に壊した入力でも `query_context` ベースの grouping / retry purge が維持されることと、shared endpoints が `subject.frame` 違いなら purge されないことを固定した。

意味:

- これは Rust 側 shortcut の除去であり、NCBI `Blast_GetQueryIndexFromContext` と `Blast_HSPListPurgeHSPsWithCommonEndpoints` の責務分割そのものへの復元である。

#### 3. protein preliminary no-right endpoint と preliminary gapped start の drift
完了。

実施内容:

- `LOSAT/src/algorithm/blastp/gapalign.rs` で、NCBI `blast_gapalign.c:4342-4344` に合わせて、protein score-only preliminary path の `found_end == FALSE` は `query_stop = init_hsp->offsets.qs_offsets.q_off`、`subject_stop = init_hsp->offsets.qs_offsets.s_off` を使うよう修正した。
- `LOSAT/src/algorithm/blastp/blast_engine.rs` で、NCBI `blast_gapalign.c:3900-3949` に合わせて `extend_preliminary_blastp_hit` の positive span / gapped start / sequence-bound checksを silent `None` ではなく fail-fast に置換した。
- `LOSAT/src/algorithm/blastp/gapalign.rs` の traceback helper でも、preliminary gapped start と traceback result を `None` で捨てる経路を削除し、NCBI 前提の invariant breach 扱いへ置換した。

意味:

- これは LOSAT 独自の `Option::None` による HSP 消失を除去し、NCBI の preliminary endpoint semantics へ戻す修正である。

### この slice で実行した確認

実行コマンド:

```bash
cargo test --manifest-path LOSAT/Cargo.toml --lib blastp::blast_engine -- --nocapture
cargo test --manifest-path LOSAT/Cargo.toml --lib blastp::gapalign -- --nocapture
cargo test --manifest-path LOSAT/Cargo.toml --lib algorithm::tblastx::blast_gapalign -- --nocapture
cargo build --manifest-path LOSAT/Cargo.toml --release --bin LOSAT
cd LOSAT && ./target/release/LOSAT blastp -query tests/fasta/AP027131.faa -subject tests/fasta/NZ_CP006932.faa -outfmt 6 -evalue 1e-5 -out /tmp/AP027131.NZ_CP006932.release.out
cd LOSAT && diff -u tests/blast_out/blastp/AP027131.NZ_CP006932.blastp.out /tmp/AP027131.NZ_CP006932.release.out
```

結果:

- `cargo test --manifest-path LOSAT/Cargo.toml --lib blastp::blast_engine -- --nocapture`: 19 件通過
- `cargo test --manifest-path LOSAT/Cargo.toml --lib blastp::gapalign -- --nocapture`: 11 件通過
- `cargo test --manifest-path LOSAT/Cargo.toml --lib algorithm::tblastx::blast_gapalign -- --nocapture`: 3 件通過
- `cargo build --manifest-path LOSAT/Cargo.toml --release --bin LOSAT`: 通過
- `AP027131.faa` vs `NZ_CP006932.faa`: zero diff にはならず、residual は `5 本` のまま不変
- gating diff が nonzero のため、`cd LOSAT/tests && bash run_blastp_comparison.sh` は未実行

### current residual

- missing: `BDV02221.1 / WP_025208963.1`
- missing: `BDV02412.1 / WP_025208950.1`
- short-vs-long: `BDV02435.1 / WP_025208655.1`
- missing: `BDV02435.1 / WP_025208654.1`
- missing: `BDV02649.1 / WP_025208424.1`

### 現時点の判断

- shared `BLAST_GetUngappedHSPList` helper の silent drop、preliminary purge の `subject.frame` 条件欠落、`context -> query_index` shortcut、protein score-only preliminary path の no-right endpoint drift は 2026-03-19 時点で除去済みである。
- それでも residual が `5 本` のまま不変である以上、今回直した admission / purge / helper 差分は current blocker の主犯ではなかった。
- この確認で、missing 4 本と `BDV02435.1 / WP_025208655.1` short-vs-long の主犯は、preliminary score-only acceptance より前の `InitHSP` 生成か、その入力順に残っている可能性がさらに高くなった。

### 次の優先順位

1. `aa_ungapped.c:439-619` と `713-801` を `LOSAT/src/algorithm/blastp/blast_engine.rs:2046-2253` に一対一で突き合わせ、`query_offset` / `curr_context` / `diag_array` / `last_hit` / `BlastSaveInitHsp` 入力順の drift を除去する。
2. それでも不変なら `blast_extend.c:325-372` と shared `BLAST_GetUngappedHSPList` 入力 (`q_off/s_off/q_start/s_start/length`) を end-to-end で再照合し、missing 4 本と longer HSP がどの stage で脱落するかを source comparison だけで特定する。
3. formatter 側、post-filter 側、oracle 依存の suppression は引き続き禁止する。

# P0 — Measurement provenance

State: COMPLETE; no release certification claim.

Program and task: TBLASTX, BLASTN/megablast, BLASTP; native and WASI command measurement.
Fixture: existing `comparison_cases.tsv` and threading fixtures. Selection is explicit in each run manifest.
Options: outfmt 6, explicit threads; Node options are separate argv arrays.
LOSAT commit and target: dirty `c0e123d771f3a58c7529aed4d3f684a1a81f4cb5`, fixed by complete `baseline-source-v2.tar.gz` and `snapshot-v2.json`; native, serial WASI, threaded WASI, threaded reactor.
NCBI BLAST+ version: 2.17.0+ (current process versions are in benchmark metadata).
NCBI source file/function/lines: `c++/src/objtools/align_format/tabular.cpp:1100–1108` (`CBlastTabularInfo::Print`), `c++/src/algo/blast/api/prelim_stage.cpp:145–188` (requested thread creation/join), `c++/src/algo/blast/blastinput/blast_args.cpp:1052–1054` (genetic code).
LOSAT owner: existing comparison and benchmark runners in `LOSAT/tests/`.
First observed divergence: historical logs and stale outputs were admissible; a successful subprocess without output could inherit prior output; failed aggregate samples could disappear.
Accepted exception: existing non-default TBLASTX local-subject genetic code; the comparison runner retains its database oracle.
Focused comparison command: `python3 -m unittest test_simple_comparison test_wasm_performance test_benchmark_wasm_threading` in `LOSAT/tests`; 36 tests passed after final clock-consumer fixes (`p0-clock-consumer-tests.log`; earlier35- and33-test records remain in `p0-review-tests.log` and `p0-final-tests.log`). Four real serial-WASI reuse evidence scenarios passed (`p0-reuse-final-test.log`).
Focused byte-diff result: covered by synthetic evidence-integrity tests, not algorithm certification.
Broader comparison: stage-specific fresh runs follow this gate.
Native/Wasm comparison: baseline builds succeeded and all three command hashes reproduce the plan’s diagnostic hashes (`builds.json`).
Baseline benchmark method and samples: fixed source/artifacts, exclusive run directories, recorded argv/hash/status; policy frozen in `snapshot.json` before candidate measurements.
Candidate benchmark method and samples: failed, missing, partial and mismatching outputs remain explicit; raw equality precedes performance summary admission.
Output checksums: per-invocation records, benchmark samples, archive hashes in `snapshot.json`.
Remaining risk or unsupported scope: P0 synthetic tests do not certify algorithms. Historical certification remains 99/145 completed, 46 incomplete.

The two previous n8 trap cases both exited successfully on the fresh baseline and showed eight spawn/ready/exited workers. `p0-traps.json` is a crash/lifecycle check; its elapsed values are diagnostic, and its success alone does not establish output parity. Parity is checked separately with current oracle runs.

The original diagnosis is preserved in `prior-diagnosis.tar.gz`. The original dirty diff is in `initial-worktree.patch.gz`; all unrelated user changes remain in place. No baseline artifacts are overwritten by candidate builds.

The clock audit subsequently identified a realtime-sensitive Python watchdog. The harness now records monotonic/realtime/boottime and GNU elapsed boundaries, verifies the monotonic deadline after early timer wake, and preserves cancellation. Two regression tests inject early wakeups. See `clock-audit.md` for the source-backed mechanism and limits. Realtime timestamps are provenance; the detailed benchmark's elapsed metric remains monotonic. The quick comparison script retains its original Bash timer and is not the adoption benchmark.

Independent review additionally found and fixed effective-locale ordering, selected NCBI search-oracle version recording, candidate Cargo.toml identity, and non-finite timeout acceptance. The finite-timeout rejection runs before run creation; the new tests cover both timeout options and oracle version/hash preservation. Earlier immutable manifests retain their original fields; source/build archives supply the adjacent build identities, rather than rewriting historical provenance.
